mod LinBranchDrop
  # PROBE L4 (negative): one branch drops the linear binder -> must REJECT.
  # "consumed on EVERY path" — a path that forgets to reply is ill-typed.
  type Box = MkBox
  type Sel = A | B
  fn pick(s: Sel, x :linear Box) -> Box = match s
    A() -> x
    B() -> MkBox
end
