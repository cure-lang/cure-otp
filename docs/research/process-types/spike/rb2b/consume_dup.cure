mod ConsumeDup
  type Box = MkBox
  type Widget = MkWidget
  type WPair = MkWPair(Widget, Widget)
  fn consume(c :linear Box) -> Widget = match c
    MkBox() -> MkWidget
  fn g(c :linear Box) -> WPair = let x = consume(c) in MkWPair(x, x)
end
