mod ReturnControl
  type Reply0 = R0
  type Reply1 = R1a | R1b
  type Req = GetCount | Ping
  fn ReplyOf(r: Req) -> Type = match r
    GetCount() -> Reply0
    Ping()     -> Reply1
  fn mk(r: Req) -> ReplyOf(r) = match r
    GetCount() -> R0
    Ping()     -> R1a
end
