# OTP formalisation ledger — implemented vs remaining, toward FULL formalisation beyond the papers

*2026-07-17. Fine-grained status of the OTP process-algebra metatheory formalised in Cure.
"Beyond the papers" baseline: **NVLang** (typing plumbing, proof sketches, toy single-actor),
**Special Delivery / Proof of Delivery** (mailbox types, commutative, operational preservation
for the type language DEFERRED), **de'Liguoro–Padovani** (commutative-regex mailbox, paper
proofs), **KWC** (ordered signals + monitors, no types). Our delta: **mechanized, dependently +
linearly typed, operational preservation over the REAL reduction, tied to inference.***

---

## PART A — IMPLEMENTED (shipped in `lib/std/otp_*.cure`, each Idris `rel=same`)

### A1. Concurrent reduction + preservation / progress
- [x] Ether→mailbox concurrent reduction (Core Erlang, Bereczky–Horpácsi–Thompson 2311.10482) — `otp_preservation.cure`
- [x] Subject reduction (tag safety) over that real reduction — `preservation` *(beyond NVLang: its preservation is over a toy step)*
- [x] Progress + type safety: a well-typed config is final or steps to a well-typed config, never stuck — `otp_safety.cure` *(NVLang has no progress theorem)*
- [x] Multi-pid INTERLEAVING preservation (async; any one process steps, others fixed) — `otp_system.cure` *(beyond NVLang's single-actor step)*
- [x] Mailbox FIFO order (arrive=snoc/end, recv=front) + `AllAccepted`-over-append lemma — `otp_fifo.cure`

### A2. Obligation 1 — per-constructor HETEROGENEOUS + LINEAR dependent reply
- [x] `ReplyOf(req)` via large elimination (per-request reply type) — `otp_proof.cure`
- [x] Linear reply capability consumed EXACTLY ONCE (QTT grade + usage; drop/dup/one-branch-drop/launder all reject) — `otp_proof.cure`
- [x] Branching reply handler (per-branch reply), drop/dup rejected — oracle `ob1_branching*`
- [x] Reply preservation through delivery = COMPOSE G1×G2 (`HasReply(r,v)`, `reify` bridge) — `otp_reply_preservation.cure`
- [x] Reply CONSERVATION, operational exactly-once (drain: complete run `(n,Z)→(Z,m) ⟹ n≡m`) — `otp_reply_conservation.cure`

### A3. Obligation 2 — clause-DERIVED pid message index
- [x] `Pid(m)` carries its handler; index derived via `spawn_actor`; `post` dispatches — `otp_proof.cure`
- [x] Effect-honest `spawn_actor`/`post` returning `Effect(...)`, threaded by `effect_bind` — `otp_send_effect.cure` *(NVLang explicitly excludes effects §8.4)*

### A4. Monitors / links / signals (G3)
- [x] Typed monitor / DOWN-as-message (`MRef` carries `Accepted(TDown)`) — `otp_monitor.cure` *(NVLang: MonitorRef in grammar only)*
- [x] Typed links + `trap_exit` dispatch (message-vs-death on the flag) — `otp_link.cure`
- [x] Demonitor (`MRef` Active/Removed; DOWN-after-demonitor unconstructible) — `otp_demonitor.cure`
- [x] Unlink (`LinkRef` Linked/Unlinked; no exit signal after unlink, keeping trap dispatch) — `otp_unlink.cure`
- [x] Reason-dependent exit propagation (`Step` indexed by `Normal`/`Abnormal`/`Kill`; normal doesn't kill a non-trapper, kill is untrappable) — `otp_exit_signal.cure`

### A5. Timers (G4) — *no paper types timers*
- [x] `send_after` typed; `TimerRef` Pending/Cancelled; fire-after-cancel unconstructible; `receive after` branch — `otp_timer.cure`

### A6. `gen_server:call` failure/totality (G6) — *no paper types the timeout*
- [x] `CallOutcome(r) = Replied(r) | Failed(CallError)`; consumer ignoring `Failed` rejected non-total — `otp_call.cure`

### A7. Registry / naming (G7) — conformance F-2/F-2c
- [x] `GenServer(q,r)` wraps a `Pid` (bare `Pid` ≠ `GenServer`); `whereis → PidOption` partiality; ignoring `NoPid` rejected — `otp_registry.cure`

### A8. Selective receive (G8 slice)
- [x] ORDERED selective receive (front-to-back scan; order = inductive structure); `preserves` + `received_accepted` — `otp_selective_receive.cure` *(mailbox papers name ordered receive as future work)*

### A9. Routing / inter-process communication
- [x] Cross-process delivery (homogeneous), routing preserves global WT — `otp_routing.cure`
- [x] HETEROGENEOUS routing (per-process interfaces via `Member`/`AllMember`) — `otp_het_routing.cure`

### A10. Supervision (C4)
- [x] `Fleet(specs)` indexed by spec list; `restart_all`/`restart_one` return same specs (type certifies restart preserves declared children) — `otp_supervisor.cure` *(NVLang never ties restart to preservation)*

### A11. Mailbox type INFERENCE (G9) — *the universally-named open problem (OTP #5364)*
- [x] Decidable core: `tag_eq`, `decide_handles`, `decide_all_handled` (total, proof-carrying) — `otp_inference.cure`
- [x] Inference LAWS: monotonicity, weakening/subtyping-transitivity, principality (`self_member`) — `otp_inference_laws.cure`
- [x] ADEQUACY, sequential + branching (`BNil`/`BRecv`/`BSend`/`BSeq`): `preservation_at`, `coverage`, `adequacy` over runs — `otp_inference_adequacy.cure` *(the operational half Proof of Delivery deferred, tied to inference)*
- [x] Pre-fixpoint bound (`lfp_le`, principality direction of Knaster–Tarski) via monotone Kleene iteration — **SHIPPED** `otp_inference_fixpoint.cure` (Idris `rel=same`)

### A12. Cross-cutting
- [x] Exhaustiveness / dead-letter prevention (C8): kernel-certified totality + exhaustiveness on every proof term
- [x] 19/22 modules pure intrinsic (indexed types + total match, no `reflexive`/`rewrite`/`with`); 35 Idris oracle pairs `rel=same`

---

## PART B — REMAINING (fine-grained), toward FULL formalisation

### B1. Near-term, in-Cure provable (small or elaborator-gated)
- [x] `unlink` (demonitor-shape over `Link`, keeping the trap dispatch) — SHIPPED `otp_unlink.cure`
- [x] EXIT **reason axis** — reason-dependent dispatch (`normal`/`abnormal`/`kill`) in `otp_exit_signal.cure`. Still open: a full reason *term* as payload, and monitor-ref correlation on DOWN
- [x] Cascading exit-signal propagation across a link CHAIN — `otp_cascade_exit.cure` (`Cascade` relation + `run_cascade` totality: terminates, survivors = suffix from first trapper). Open: a general link GRAPH (fan-out) vs the chain slice
- [ ] **E1** sibling-context refinement on `match` (elaborator ergonomics — removes the data-first tax) — spec `2026-07-17-proof-authoring-elaborator-ergonomics-design.md`
- [ ] **E3 / task #15** cross-module resolution of implicit-carrying stdlib fns (lets proof lemmas factor across modules)
- [ ] **E4** partial-app codegen (eta-expand explicit-arg partial applications)

### B2. Recursion fixpoint — genuine in-Cure research (Knaster–Tarski / Kleene)
- [x] `BRec` behaviour representation (`RBody` with single recursion variable `RVar`) + syntax-derived transfer `tset(body, I)` + `infer(BRec) = rec_infer` — **SHIPPED** `otp_recursive_transfer.cure` (Idris `rel=same`)
- [x] transfer MONOTONICITY generic over syntax (`tset_mono`, induction on the body; `orb_mono`/`join_mono`/`setbit_mono`) — the hypothesis `map_lfp_le` needs — **SHIPPED** `otp_recursive_transfer.cure`
- [x] fixed-point property at a syntactic transfer (`rec_fixed = map_lfp_le(tset(body,-), tset_mono(body))`) — **SHIPPED** `otp_recursive_transfer.cure`
- [x] `lfp` construction via bounded Kleene iterate `iterate(f, ⊥, height)` — **SHIPPED** (`rec_infer = iter(tset(body,-), 4)`)
- [x] `?lfp_le` pre-fixpoint bound — **SHIPPED** `otp_inference_fixpoint.cure`
- [x] `?le_lfp` (`le_sInf`: `(∀b. f(b) ⊑ b → a ⊑ b) → a ⊑ lfp`) — SHIPPED `otp_finite_fixpoint.cure` (Idris `rel=same`): `lfp = iter(f,4)` is itself a pre-fixed point (`map_lfp_le`), so instantiate the hypothesis there. With `lfp_le` this is the full universal property `lfp = ⨅{b | f(b) ⊑ b}` — Knaster–Tarski over the `IF` lattice is COMPLETE.
- [x] **STABILIZATION**: `iterate(f, ⊥, |Tag|)` IS a fixed point — finite-height counting (interface size ≤ 3, strict-increase-or-fixed) — **SHIPPED** `otp_finite_fixpoint.cure` over the height-3 `IF` lattice (Idris `rel=same`). *The hard half, mathlib `monotone_chain_condition` port.*
- [x] `?map_lfp` (fixed-point property `f(lfp) ⊑ lfp`) — **SHIPPED** `map_lfp_le` in `otp_finite_fixpoint.cure`
- [x] `IF ↔ TagList` bridge (`denote`, `denote_complete`, `sub_allhandled`) — **SHIPPED** `otp_interface_bridge.cure` (Idris `rel=same`); transfers `map_lfp_le`'s `Sub` conclusion to `AllHandled` membership, so the finite fixpoint is usable in the adequacy representation
- [x] `?is_least` principality — `lfp_le` (Kleene pre-fixpoint bound over the `IF` lattice) + `rec_is_least` (the inferred interface is contained in every self-consistent interface; with `rec_fixed` it is the LEAST fixed point) — **SHIPPED** `otp_recursive_transfer.cure` (Idris `rel=same`)
- [x] BRec **coverage** — every direct send tag of a recursive behaviour is a member of its inferred fixed-point interface (`RSendsIn`, `tset_covers`, `brec_covers`), in both bit-set and membership (`brec_handles : Handles(t, denote(rec_infer(body)))`) form — **SHIPPED** `otp_recursive_adequacy.cure` (Idris `rel=same` on the `tset_covers` induction; the membership bridge reuses the oracle-verified `denote_complete` + `rec_fixed`). Canonical `Tag` now shared across `InterfaceBridge`/`RecursiveTransfer`/`RecursiveAdequacy`.
- [x] BRec **adequacy** proper — every config reachable by running a recursive behaviour is well-typed at `denote(rec_infer(body))` (`preservation_at` + `adequacy_at` + `rec_send_step` + `rec_adequacy`) — **SHIPPED** `otp_recursive_run.cure` (Idris `rel=same` on the operational core; `rec_send_step` justifies recursive sends via `brec_handles`). BRec inference is now operationally sound end to end for the finite universe: coverage (fundamental theorem) + preservation (operational closure).
- Scaffold: `scaffolds/inference_fixpoint.cure`; shape doc `2026-07-17-mailbox-inference-fixpoint-shape.md`

### B3. Counting / multiplicity fragment — commutative-regex mailbox types (Special Delivery, de'Liguoro–Padovani)
*(the tag-SET model does not reach this; it is a set, not a multiset with counts)*
- [x] Mailbox types as COMMUTATIVE REGEX (patterns `PZero`/`POne`/`PAtom`/`PPlus`/`PTimes`/`PStar`) over MULTISETS (Parikh vectors `MkMS(Nat,Nat,Nat)`), with the `Accepts` relation and the COMMUTATIVE-MONOID laws of `(Pat, PTimes, POne)`: `times_comm` (`E·F ≡ F·E`), `times_assoc`, `one_times` (unit), plus `plus_comm` (choice) — all via `msadd` arithmetic (`msadd_comm`/`msadd_assoc`/`msadd_zero_left` over Nat `plus_comm`/`plus_assoc`) — **SHIPPED** `otp_mailbox_pattern.cure` (Idris `rel=same`). The counting model the tag-set inference cannot express.
- [x] Brzozowski derivatives for pattern matching: `nullable` + `nullable_sound`; the commutative `deriv(E,t)` and **`deriv_sound`** — the SOUNDNESS half of the fundamental theorem, `Accepts(deriv(E,t), m) → Accepts(E, m ⊎ {t})` — proved for the full pattern algebra incl. `PStar` (constructive; relocates the peeled `{t}` with the bag-algebra laws) — **SHIPPED** `otp_mailbox_pattern.cure` (Idris `rel=same`). `nullable_complete` NOW SHIPPED (2026-07-18, `51844425`, Idris `rel=same`): the converse of `nullable_sound` — `Accepts(pat, MkMS(Z,Z,Z)) → nullable(pat) = T`, giving the full iff for nullability. The claimed "elaborator gap" was ROUTED AROUND with no kernel/E change: `nc_gen` GENERALIZES the multiset index and carries the equation `m = MkMS(Z,Z,Z)` as an explicit argument, so matching the `Accepts` derivation refines `m` per ctor into a usable inversion (`ATimes`, via `msadd_zero_l/r` = congruence-projection + `plus` no-confusion) or an absurd one (atom ctors, `S(Z)` index, `match q`). `deriv_complete` + `matches_word_complete` NOW SHIPPED (2026-07-18, `8b237eef`, Idris `rel=same`) — the FULL Brzozowski fundamental theorem is CLOSED in both directions: `w` is Parikh-accepted by `e` iff `nullable(dfold(e, w))`. The derivative converse is `dc_gen` (index-generalized induction on the `Accepts` derivation). The crux `msadd_split` decides "which side did the single tag land on" via coordinate arithmetic (`plus_succ_split`: `a + b = S(n)` ⟹ `a` or `b` is a successor) and reconstructs the remainder; the hard `PStar` case (tag in the star TAIL) deconstructs the recursive derivative and rebuilds it with the mailbox-monoid laws, prepending the head occurrence back into the star. NO kernel/E change — the "stuck-index equation" gap is routed around by index generalization. Row COMPLETE.
  - **Elaborator parity gaps found (cataloged for the ergonomics spec):** (1) matching an `ATimes`-style ctor whose index is a stuck function-app (`msadd(m1,m2)`) does not retain the index equation `m = msadd(m1,m2)` as a usable proof — blocks any inversion. (2) SEQUENTIAL-MATCH refinement: `match pat` then a separate `match acc → … → ATimes` does not propagate the second match's index refinement into the goal the first match already specialized (Idris matches all patterns simultaneously per clause; Cure's `match` is independent per scrutinee). WORKAROUND for (2): helper-delegation — match the evidence as a function PARAMETER (no prior data-match freezing the goal). Used in `deriv_sound`'s `ds_times`/`ds_star`.
- [x] KLEENE STAR FIXPOINT LAW `*E ≡ 𝟙 ⊕ E·(*E)` (`star_unfold`/`star_fold`, both directions, pure constructor rearrangement) — **SHIPPED** `otp_mailbox_pattern.cure` (Idris `rel=same`).
- [x] SEMIRING left-DISTRIBUTIVITY `E·(F⊕G) ≡ (E·F)⊕(E·G)` (`dist_fwd`/`dist_bwd`, both directions) — **SHIPPED** `otp_mailbox_pattern.cure` (Idris `rel=same`).
- [x] ADDITIVE monoid + ANNIHILATOR: `⊕` associative (`plus_assoc_fwd`/`bwd`) with unit `PZero` (`plus_zero_fwd`/`bwd`), and `PZero` absorbs `·` (`zero_times`) — `(Pat, ⊕, 𝟎, ·, 𝟙, *)` is a commutative KLEENE ALGEBRA up to acceptance — **SHIPPED** `otp_mailbox_pattern.cure` (Idris `rel=same`).
- [x] Pattern SUBTYPING (inclusion): inductive `Incl(E,F)` (refl, `𝟎`-least, choice lub/injections, `·`/`*` monotone, transitive) + `incl_sound` (implies semantic inclusion — a mailbox typed `E` is usable where `F` is expected) — **SHIPPED** `otp_mailbox_pattern.cure` (Idris `rel=same`). Note: this is the SOUND syntactic subtyping; the DECISION procedure for semantic inclusion needs the Presburger solver (out of TCB).
- [x] Derivative MONOTONICITY under subtyping (`deriv_mono : Incl(E,F) → Incl(deriv(E,t), deriv(F,t))`, pure `Incl` recursion) — **SHIPPED** `otp_mailbox_pattern.cure` (Idris `rel=same`).
- [x] BRZOZOWSKI MATCHING (operational membership decision): `dfold` folds the derivative along a message `Word`, matching = residual `nullable`; `matches_word_sound` proves it sound (`nullable(dfold(E,w))=T ⟹ Accepts(E, parikh(w))`) — **SHIPPED** `otp_mailbox_pattern.cure` (Idris `rel=same`). The operational use of the derivative; completeness is E9-blocked.
- [~] Conformance (`reliable`/`usable`, de'Liguoro–Padovani Def. 10) — SAFETY ESSENCE SHIPPED `74903933` (Idris `rel=same`, in `otp_mailbox_pattern.cure`): `reliable` (a conforming mailbox `Accepts(E,m)` is never STUCK — either satisfied-and-empty with `nullable(E)`, or can consume a pending message leaving a conforming residual, via `deriv_complete`) + `drain` (USABILITY — a conforming mailbox is fully consumable to a satisfied empty state; terminating `Drain` run recursing on the multiset size measure, which decreases definitionally). Captures the reliability/usability safety over the actual `Pat` mailbox types. OPEN: the full Def-10 process-calculus formulation (over a labelled process-transition system, not just accepted-content consumption) is broader; and bidirectional constraint generation / evolving-protocol inference remain (latter gated on the out-of-TCB Presburger solver).
- [ ] Bidirectional constraint GENERATION from a behaviour
- [ ] **Presburger / semilinear (Parikh) SOLVING** — external Z3/omega backend, **OUT OF TCB** (untrusted lint per the SMT trust-boundary decision), not a kernel proof
- [~] Session-types-into-mailboxes encoding (de'Liguoro–Padovani §4.3–4.4): binary session types + DUALITY foundation — `dual`, `dual_involution` (dual is an involution), `Compat` + `compat_dual` (compatible endpoints are exactly dual endpoints, i.e. communication safety = duality) + `dual_compat` (the CONVERSE: every type IS compatible with its dual, `Compat(S, dual(S))` — closing the "compatible ⟺ dual" equivalence in BOTH directions) + `compat_sym` (compatibility is SYMMETRIC: `Compat(l,r) ⟹ Compat(r,l)`, the two endpoints are interchangeable) + `compat_unique` (compatible peer is UNIQUE: `Compat(l,r)` and `Compat(l,r2)` ⟹ `r = r2` — both equal `dual(l)` and `dual` is injective, so a well-typed channel determines its other endpoint's type unambiguously) — **SHIPPED** `otp_session.cure` (Idris `rel=same`). [Attempted but DEFERRED: the mailbox-encoding monoid-homomorphism `recvs(seq(s,t)) = msum(recvs s, recvs t)` — blocked by a KERNEL normalizer incompleteness (conversion won't reduce a reducible arg in a stuck application's spine, e.g. `recvs(LEnd)` inside a stuck `msum`); recorded as finding **K1** in the ergonomics spec, TCB HARD-STOP.] plus `SStep` + `session_preservation` (subject reduction: a communication step between compatible endpoints yields compatible endpoints — a safe session stays safe as it runs). Plus `SRun` + `session_run_safe` (safety over a WHOLE run: a session from compatible endpoints stays compatible through every step — never gets stuck on a type mismatch). Plus `Progress` + `session_progress` (PROGRESS: a compatible pair is either finished (`SEnd`/`SEnd`) or a communication redex — a well-typed session never deadlocks; with preservation this is full type safety, well-typed ⟹ never stuck). Plus `sstep_sym`/`srun_sym` (STEPS AND RUNS REVERSE UNDER ENDPOINT SWAP: `SStep(l,r,l2,r2) ⟹ SStep(r,l,r2,l2)` and likewise for `SRun` — send↔receive, select↔offer; the operational face of `compat_sym`, so the two participants' views of a session's execution are mirror images). Plus `compat_terminates` (NORMALISATION: every compatible finite session runs all the way to `(SEnd, SEnd)` — an explicit full `SRun` built by structural induction; packages progress + preservation + termination, so a well-typed session never deadlocks, never mistypes, and always terminates). The construction uses the explicit-`tail` pattern to pin `SRStep`'s floating target indices past the E6-residual construction-position gap (documented in the ergonomics spec). Plus `compat_terminates_len` (TERMINATION TIME IS EXACT: `srun_len(compat_terminates(c)) = sdepth(l)` — the canonical terminating run takes EXACTLY `sdepth(l)` communication steps, sharpening normalisation from "reaches completion" into a precise step count fixed by the endpoint's type depth; induction on the compatibility derivation, one step per send/receive/chosen-branch via `snat_cong`). Plus RECURSIVE session types (`Otp.Meta.RecursiveSession`): `RSType` with `μX`/`RVar`, `subst`/`unfold`/`rdual`; `rdual_involution`, `subst_dual` (dual distributes over substitution), and the headline `dual_unfold_commute` (`dual(unfold(μX.S)) = unfold(μX.dual(S))` — a looping protocol's endpoints stay dual across every unfolding); generalized to `rdual_unfold` (`dual(unfold(S)) = unfold(dual(S))` for EVERY shape, of which `dual_unfold_commute` is the `RMu` instance). Plus MULTIPARTY session types (`Otp.Meta.MultipartySession`): `Global` protocol type + `project(G, role)` to per-role `Local` types; `projection_duality` proves COHERENCE (a two-party global projects to DUAL endpoints — global well-formedness yields local communication safety, Honda–Yoshida–Carbone). Plus `GStep` + `twoparty_preserved` (GLOBAL SUBJECT REDUCTION: firing the head interaction of a well-formed two-party protocol yields a well-formed two-party protocol — so with `projection_duality` the projected endpoints stay coherent through every step of the protocol's execution). Plus `GProgress` + `global_progress` (GLOBAL PROGRESS: a well-formed two-party protocol is either finished or its head can fire — never deadlocks; with `twoparty_preserved` this is full safety for the global protocol). Plus OPERATIONAL CORRESPONDENCE (`LStep` + `proj_sender_steps`/`proj_receiver_steps`): when the global fires its head `RA → RB : t`, the SENDER's projection takes a local SEND step and the RECEIVER's a local RECEIVE step, both to the continuation's projection — so projection is sound w.r.t. reduction (global steps are mirrored by matching local steps at each participant, not merely statically coherent). Plus `grun_preserves` (GLOBAL SAFETY OVER A RUN: `TwoParty(g)` + `GRun(g,g2)` ⟹ `TwoParty(g2)` — a protocol started well-formed stays well-formed through EVERY step, so with `projection_duality` the projections stay coherent at every reachable point; multi-step strengthening of `twoparty_preserved`). Plus `GRun` + `twoparty_terminates` (GLOBAL NORMALISATION: every well-formed two-party protocol runs all the way to `GEnd` — a full `GRun` built by structural induction firing the head at each step; with progress + subject reduction, the global protocol never deadlocks, stays well-formed, and always terminates; uses the explicit-`tail` construction pattern). Plus the SESSION→MAILBOX ENCODING (`Otp.Meta.SessionMailbox`, the de'Liguoro–Padovani headline): an endpoint is realized as a process with a MAILBOX = the commutative multiset (`MS`) of tags it receives; `recvs`/`sends` measure the received/emitted multisets, and `recvs_dual` proves ENCODING FIDELITY — `recvs(dual(L)) = sends(L)`, the mailbox an endpoint must service is exactly what its dual peer sends (with the mirror `sends_dual`), so the encoding preserves communication safety. Plus FORK/JOIN (parallel session composition `LPar`): an endpoint may run two independent sub-sessions in parallel, whose mailbox is the multiset SUM (`msum`) of the components'; `recvs`/`sends`/`dual` extend componentwise and fidelity (`recvs_dual`/`sends_dual`) still holds via `msum_cong` — so parallel composition preserves the encoding's communication safety. Plus ENCODING BALANCES COMPATIBILITY (`Compat` over `Local` + `compat_recv_send`/`compat_send_recv`): for compatible endpoints one side's mailbox multiset is exactly the other's emissions (`recvs(l) = sends(r)` and `sends(l) = recvs(r)`, fork/join included) — the encoding turns a well-typed (compatible) session into a pair of mailboxes that exactly service each other's traffic, communication safety at the mailbox level. Plus THE ENCODING IS A MONOID HOMOMORPHISM (`seq` + `recvs_hom`/`sends_hom`): sequential composition of sessions maps to multiset SUM of mailboxes — `recvs(seq(s,t)) = msum(recvs(s), recvs(t))` (and `sends` likewise), so the mailbox of a composite protocol is exactly the union of its parts'. **Unblocked by the K1 KERNEL FIX** (`fix(core)` `dee86c0b`: `Normalise.reduce_unfolded`'s A6 freeze now fires consistently, so `msum(recvs(LEnd), …)` and `msum(empty, …)` compare equal); supporting algebra `add_assoc`/`msum_assoc`/`bump_msum_dist`/`msum_empty_l`. Plus the FULL ALGEBRAIC STRUCTURE: `(MS, msum, empty)` is a COMMUTATIVE MONOID (`msum_comm` via `add_comm`, `msum_assoc`, two-sided unit `msum_empty_l`/`msum_empty_r`) and `(Local, seq, LEnd)` is a MONOID (`seq_assoc`, two-sided unit `seq(LEnd,s)=s` definitional + `seq_end_r`) — so with `recvs_hom`/`sends_hom` preserving the unit (`recvs(LEnd)=empty`), `recvs`/`sends` are genuine MONOID HOMOMORPHISMS from the session monoid to the commutative mailbox monoid (Nat lemmas `add_zero_r`/`add_succ_r`/`add_comm`). Plus MAILBOX TYPES FORGET ORDER (the point of the encoding): `recvs_par_comm` (fork/join is order-insensitive, `recvs(LPar(a,b))=recvs(LPar(b,a))`) and `recvs_seq_comm`/`sends_seq_comm` (`recvs(seq(a,b))=recvs(seq(b,a))` even though `seq` is NOT commutative — the mailbox is a commutative BAG, so swapping two composed sub-protocols leaves the expected-message multiset unchanged). This DERIVES why mailbox types can be commutative, from the monoid homomorphism + `msum_comm`. Plus COMPOSITIONALITY (`mailbox_seq_cong`/`mailbox_par_cong`): substituting mailbox-EQUIVALENT sub-protocols (`recvs(s)=recvs(s2)`) leaves the composite mailbox unchanged — the mailbox of a protocol depends only on its parts' mailboxes, not their internal structure, so sub-protocols can be type-checked independently and composed (modularity of mailbox typing). Plus THREE-ROLE PROJECTION (`Otp.Meta.ThreeParty`, the tractable slice of >2-role coherence — full N-role coherence needs branch-MERGE, deferred): three roles `RA`/`RB`/`RC`; `bystander_ab` proves a role party to none of a bilateral protocol's messages projects to `LEnd` (NON-PARTICIPATION — the third role can be spawned or omitted freely; the message-skip discharges definitionally since `RA`/`RB` ≠ `RC`), and `bilateral_duality` proves an `RA`/`RB`-bilateral protocol projects those two to DUAL endpoints unaffected by `RC`'s presence (projection is LOCAL). Plus SESSION SUBTYPING (`Otp.Meta.SessionSubtyping`, Gay–Hole safe substitutability): `Sub(s,t)` = an endpoint typed `s` is usable where `t` is expected. The crux is OPPOSITE width variance of the two choice forms — internal choice `SSelect` (⊕) is COVARIANT in its label set (a subtype offers FEWER labels, `s1 ⊆ s2`), external choice `SBranch` (&) is CONTRAVARIANT (offers MORE, `s2 ⊆ s1`); send/receive continuations covariant. Label sets are pointwise boolean orders (`LSub` over `BLe`) so the ⊆ lemmas are clean inductions not boolean case-bashing. Proven: `sub_refl` + `sub_trans` + `sub_antisym` (Sub is a PARTIAL ORDER — mutual subtypes are structurally equal, via `ble_antisym`/`lsub_antisym` down through the boolean-order and label-set layers; the external-choice case composes the label witnesses in FLIPPED order for its contravariance) and `sub_dual_antitone` (duality REVERSES subtyping — `dual` swaps ⊕/& so the choice cases exchange constructors, making subtyping sound for both channel endpoints). Plus the SOUNDNESS capstone `narrow` (SUBSTITUTABILITY: `Sub(s,s2)` and `Compat(s2,t)` ⟹ `Compat(s,t)` — a subtype can be safely substituted in any compatible session; each choice case composes the label witnesses with `lsub_trans` in its variance-dictated order, `s ⊆ s2 ⊆ t` for select, `t ⊆ s2 ⊆ s` for branch) — the theorem that gives subtyping its meaning. Choices carry a shared continuation (faithful simplification of per-label continuations); n-ary per-label branching deferred. Plus SESSION DELEGATION / HIGHER-ORDER CHANNELS (`Otp.Meta.SessionDelegation`): `SDeleg(a, k)` sends a channel of session type `a` then continues as `k`, `SResume(a, k)` receives it — the distinguishing higher-order session feature (e.g. a balancer handing a live connection to a worker). KEY subtlety: the delegated payload `a` is transferred AS-IS (NOT dualized — the receiver takes over the exact endpoint); only the carrier continuation is dualized. `dual_involution` still holds (carried `a` copied verbatim through both duals) and `compat_dual` still holds (delegate meets resume on the same `a`), so communication safety = duality even with first-class channels. Plus the SESSION↔OTP-CALL BRIDGE (`Otp.Meta.CallSession`): `gen_server:call` as a DEPENDENT binary session whose reply tag is COMPUTED from the request (`reply_for(req)`, the session-typed face of `ReplyOf(req)`); `client(req)`/`server(req)` are the endpoints, `call_duality` proves `client(req) = dual(server(req))` for EVERY request by reflexivity (uniform dependent well-formedness — `dual` never inspects the stuck `reply_for(req)`), and `call_compat` proves the dependent request/reply exchange is compatible for all requests. Plus the VALUE-LEVEL dependent call result (`Otp.Meta.CallResult`, G6): a call returns `Ok` carrying a reply whose VALUE TYPE is computed from the request via LARGE ELIMINATION `RepVal(reply_for(req))` (RGet↦Nat, RPut↦Bit) or `Err(CallError)` (Timeout/NoProc/ServerDied); `get_reply`/`put_reply` witness that the success payload really has the request-determined type (a `Bit` where a `Nat` is required is rejected), and `describe` is total only because it handles both outcomes — a consumer that ignores failure is rejected non-total, so a failed call can't be silently dropped. This is the value-level face of `ReplyOf(req)`. Plus THE BRANCH-MERGE OPERATOR + CHOICE PROJECTION COHERENCE (`Otp.Meta.BranchMerge`, the deferred N-role machinery): global protocols with CHOICE (`GCho(from,to,tL,gL,tR,gR)`) and local types with internal/external choice (`LSel`/`LBra`). The `merge` operator — TOTAL, `LErr` on conflict — is the crux of multiparty projection: projecting onto a role that's a BYSTANDER to a choice must combine its two branch-behaviours, and the defining rule is that DIFFERING RECEIVES UNION into an external choice (`merge(LRecv(a,·), LRecv(b,·)) = LBra(a,·,b,·)` for `a≠b` — the bystander offers both labels since it learns which branch was taken by which label it receives), while differing SENDS conflict (`LErr`). `merge_idem` proves `merge(L,L)=L` (a type merges with itself — the identical-bystander base of coherence). The headline `choice_duality` proves `Coherent(g) ⟹ project(g,RA) = dual(project(g,RB))` — a coherent RA/RB protocol WITH CHOICE projects its two active roles to DUAL `LSel`/`LBra` endpoints (`dual` swaps internal⊕external choice), so `Otp.Meta.ThreeParty`'s `bilateral_duality` is now LIFTED to branching protocols, in a system carrying the branch-merge operator. FINDING: the total-merge-with-`LErr` design was essential — an initial partial `LOpt` projection forced the coherence proof to name the matched `Coherent` constructor's existential index `k` (which is UNnameable in source: `:unknown_global`; and making the continuation implicit erases it, so it can't be matched: `:erased_used_relevantly`). Making `merge`/`project` total (so `dual` reduces directly on `Local`) let `choice_duality` go through by plain constructor congruence exactly like `bilateral_duality`, no index-naming. **SHIPPED** `otp_branch_merge.cure` (Idris `rel=same`). Plus N-ROLE BYSTANDER PROJECTABILITY (the branch-merge CONSUMED in a coherence proof): the MERGEABILITY relation `Mergeable(x,y)` — the standard multiparty notion characterizing exactly when two bystander branch-behaviours combine (same sends / same-tag receives merge pointwise; DIFFERING receives `a≠b` union via a `tag_eq(a,b)=F` witness; choices merge on matching labels) — with `merge_ok` (`Mergeable(x,y) ⟹ merge(x,y) ≠ LErr`, i.e. mergeable types have a DEFINED merge; the differing-receive case cased 9-way so `tag_eq` computes and the impossible diagonal refutes the `a≠b` witness). Then the headline `bystander_defined` (`WF(g) ⟹ is_err(project(g, RC)) = F`): a WELL-FORMED global (predicate `WF` with genuine bystander RC receiving from RA/RB, and a `Mergeable` witness carried at each choice — the multiparty well-formedness SIDE CONDITION) projects the bystander to a DEFINED local type, never a conflict. The CHOICE case is `project(g,RC) = merge(project(gL,RC), project(gR,RC))`, discharged by `merge_ok(mg)` — so the branch-merge operator is CONSUMED in the coherence proof and multiparty projection is TOTAL on well-formed protocols. So both multiparty coherence facts now hold WITH CHOICE: active roles project DUAL (`choice_duality`) and bystanders project DEFINED (`bystander_defined`). Plus THE CORRECTNESS JUSTIFICATION — `merge_sub_l`/`merge_sub_r` prove BRANCH-MERGE IS A LOWER BOUND in subtyping: `Mergeable(x,y) ⟹ merge(x,y) <: x` AND `<: y`. The merged bystander type is a SUBTYPE of BOTH branches, so a role typed `merge(x,y)` is safely usable in place of EITHER branch's expected type — the semantic reason branch-merge is sound. A `Sub` relation (Gay–Hole variance: send/receive/choice continuations covariant) with the crux rule `SubBraL`/`SubBraR` — an external choice offering `{a,b}` SUBSUMES just receiving `a` (or `b`), since it does everything the smaller type can plus more; so the receive-union `LBra` is below each single `LRecv`. `sub_refl` (reflexivity) supplies the matching-action continuations. FINDING: the nullary `SubBraL : Sub(LBra(a,k,b,kb), LRecv(a,k))` (identity continuation, indices inferred from the goal) dodges naming the `Mergeable` constructor's existential continuation `k` — the same index-nameability route-around as the total-merge design. Item (2) substantially DONE (branch-merge operator + choice duality + mergeability + projectability + subtyping lower-bound). **SHIPPED** `otp_branch_merge.cure` (Idris `rel=same`). Plus PROJECTION TOTALITY FOR ALL THREE ROLES (`ebfac984`, Idris `rel=same`): `active_a_defined`/`active_b_defined` (the active roles RA/RB always project to a defined non-`LErr` local — `merge` is only reached for a bystander, which RA/RB never are in a WF choice) + `all_projections_defined` bundling all three into one `AllDefined` witness — multiparty projection is TOTAL for EVERY role on a well-formed 3-role global (no role ever gets a conflict; the definedness face of full mutual coherence). Plus OPERATIONAL PROGRESS + TERMINATION (`39ea652b`, Idris `rel=same`): `GStep` (global reduction — fire head message or take a choice branch), `gstep_preserves` (SUBJECT REDUCTION — WF preserved by every step), and `branch_terminates` (`WF(g) ⟹ GRun(g, GEnd)` — every well-formed branching global runs all the way to GEnd, so NEVER DEADLOCKS at the global level and always terminates; extends two-party `twoparty_terminates` to the branch-merge grammar). Plus DUALITY PRESERVED UNDER REDUCTION (`bd1fe95b`, Idris `rel=same`): `cstep_preserves` (coherence subject reduction) + `duality_preserved` (`Coherent(g)` + `GStep(g,g') ⟹ project(g',RA)=dual(project(g',RB))`) — the two active roles' endpoints stay DUAL at every reachable state, so communication safety is preserved operationally, not just established statically. So the 3-role branching system is now statically coherent (projection totality + choice_duality + bystander_defined) AND operationally safe (subject reduction + normalisation + duality-preservation). Plus the FULL PER-ROLE OPERATIONAL CORRESPONDENCE (`42f32405`+`e4d800d1`+`9173c821`, Idris `rel=same`): a `LStep` local-reduction relation and, for a firing global, a matching local step at EVERY role — `proj_sender_steps`/`proj_receiver_steps` (message endpoints take LSend/LRecv, receiver gated by a `role_eq(from,to)=F` witness so the diagonal is refuted), `proj_chooser_steps`/`proj_offerer_steps` (choice endpoints take LSel/LBra); the BYSTANDER halves `proj_bystander_msg` (a role party to neither endpoint of a message is UNCHANGED — the multiparty NON-INTERFERENCE / confluence lemma, `project(GMsg(from,to,t,k),r)=project(k,r)`, 27-way concrete role casing since `rewrite` can't see `role_eq` under the unreduced `project` — new elaborator finding E12) and `bystander_cho_sub_left`/`bystander_cho_sub_right` (a choice bystander SUBTYPE-steps to the taken branch: its projection `merge(pL,pR)` is `<:` the chosen branch's projection, discharged by `merge_sub_l`/`merge_sub_r` on the `WFCho` mergeability witness). So projection is sound w.r.t. reduction for ALL roles at ALL steps, not merely statically coherent. Plus SUBTYPING IS A PREORDER (`f40beef6`, Idris `rel=same`): `sub_trans : Sub(a,b) → Sub(b,c) → Sub(a,c)` by induction on both derivations — with `sub_refl` this makes local session `<:` a preorder, the algebraic precondition for using it as a substitution principle. The proof EXPOSED a real defect: the nullary rigid width rules `SubBraL`/`SubBraR` are NOT closed under composition (`SubBra;SubBraL` needs a COVARIANT continuation); fixed by adding the faithful Gay–Hole covariant width rules `SubBraLcov`/`SubBraRcov` (carrying a `Sub(k,k2)` continuation proof), keeping the nullary forms as their reflexive instance so `merge_sub_l`/`merge_sub_r` are unchanged. Plus CONFIGURATION-LEVEL SESSION FIDELITY + DEADLOCK-FREEDOM (`908bb574`, Idris `rel=same`): a running system is the TRIPLE of the three roles' local projections (`Config = MkConfig(RA,RB,RC)`, `config(g)` snapshots it); `CStep` reduces a configuration with each role individually JUSTIFIED (`Justified`: an acting endpoint by a real `LStep`, an uninvolved role by staying `JSame`, a choice bystander by a SUBTYPING step `JSub` — its merged type absorbs into the taken branch). `config_fidelity` (`WF(g)` + `GStep(g,g2) ⟹ CStep(config(g), config(g2))`) proves every global step is mirrored by one justified configuration step — Honda–Yoshida–Carbone SESSION FIDELITY, composing every per-role step lemma (all witnesses fall out of existing `LStep`/`Sub` constructors with goal-inferred indices; only the right-choice steppers `LStSelR`/`LStBraR` were added). `config_deadlock_free` (`WF(g) ⟹ ConfigProgress(config(g))`) is the corollary: the projected configuration of a well-formed protocol is either DONE (all `LEnd`) or STEPS — never stuck. So the 3-role branching system's projections form a DEADLOCK-FREE configuration, the running-system-level face of coherence. Plus SUBTYPING SIMULATES LOCAL REDUCTION (`0fcc1599`, Idris `rel=same`): `sub_step_l` proves `Sub(a,b)` + `LStep(b,b2) ⟹ StepTo(a,b2)` (∃`t`. `LStep(a,t)` ∧ `Sub(t,b2)`) — if a subtype's supertype takes a local step, the subtype takes a MATCHING step into a subtype of the result. This is the OPERATIONAL soundness of the subtyping preorder (`sub_trans` was the algebra, this is the dynamics): a process typed by a subtype safely follows wherever its spec leads. Send/select cases match structurally; the width cases show a branch (`LBra`) receiving the exact label a single `LRecv` expects and continuing below it. Plus MULTIPARTY SUBSTITUTABILITY (`7d0ccad1`, Idris `rel=same`): `config_subst` proves subtyping is SOUND FOR IMPLEMENTATION REFINEMENT — if each role is implemented by a subtype of its protocol projection (`ConfigSub(impl, config(g))`), then whenever the well-formed protocol takes a global step, every role's implementation takes a JUSTIFIED step that STAYS a subtype of the new projection (`ConfigStep(impl, config(g2))`). So a deadlock-free protocol stays deadlock-free under ANY per-role subtype refinement — the Gay-Hole substitution principle lifted to the multiparty running system. Per role (`justified_sub`): an acting endpoint follows via `sub_step_l`, a staying role keeps its witness, a subtype-stepping bystander composes via `sub_trans` — so it composes `config_fidelity` + `sub_step_l` + `sub_trans` into the multiparty subtyping-soundness capstone. (Takes `g2` explicit so the spec continuations `project(g2,role)` are writable and thread as `sub_step_l`'s explicit `b2`, routing around elaborator finding E13 — the reflexive-constructor `SubRefl : Sub(l,l)` reconstruction gap that blocks the implicit-continuation form.) Plus RECURSIVE MULTIPARTY PROTOCOLS (`66fdd716`, new probe `otp_multiparty_recursive`, Idris `rel=same`): globals/locals with single-variable recursion (`GRec`/`GVar`, `LRec`/`LVar`). The headline `unfold_commute` proves PROJECTION COMMUTES WITH RECURSION UNFOLDING — `project(unfold(GRec body), r) = unfold_local(project(GRec body, r))` — so a looping protocol's per-role behaviour is stable across every unfolding (the multiparty analog of the binary `dual_unfold_commute`). It rests on `project_subst_hom`: projection is a SUBSTITUTION HOMOMORPHISM, `project(gsubst(g,s),r) = lsubst(project(g,r), project(s,r))`, by induction on `g` (the message case cases all three roles concretely so `role_eq` reduces; sender/receiver recurse under `lsend`/`lrecv` congruence, a bystander recurses directly). This is the recursion slice WITHOUT choice; recursive projection through the branch-MERGE (choice under recursion) is the natural follow-on. Plus RECURSIVE TWO-PARTY COHERENCE (`ea816e39`, Idris `rel=same`): DUALITY on recursive local types (`dual` over `LEnd/LSend/LRecv/LRec/LVar`) with `dual_involution` (involution), `dual_subst_hom` (dual distributes over substitution) and its corollary `dual_unfold_commute` (DUAL COMMUTES WITH UNFOLDING — a looping endpoint's dual is the dual endpoint looping); then `projection_duality` proves a coherent RA/RB recursive protocol projects its two active roles to DUAL endpoints (`project(g,RA) = dual(project(g,RB))`) carried through the `GRec`/`GVar` cases under `LRec`/`LVar` — and since it holds at every coherent (hence every unfolded) global, duality is PRESERVED across unfolding: Honda-Yoshida-Carbone coherence lifted to recursive protocols. Plus THE CRUX OF CHOICE-UNDER-RECURSION RESOLVED (`0cee2f70`, new probe `otp_merge_recursion`, Idris `rel=same`): `merge_lsubst_commute` proves `Mergeable(x,y) ⟹ merge(lsubst(x,s), lsubst(y,s)) = lsubst(merge(x,y), s)` — MERGE COMMUTES WITH SUBSTITUTION for mergeable branches — over a `Local` with BOTH choice (`LSel`/`LBra`) and recursion (`LRec`/`LVar`), `merge`/`Mergeable` extended with `MgVar`/`MgRec`. KEY INSIGHT resolving the flagged obstacle: the merge-subst homomorphism does NOT need a separate guardedness predicate; MERGEABILITY itself is the right hypothesis. The feared bad case (substituting into a bare `LVar` merged with a concrete type could change mergeability) NEVER arises because that pair is exactly NON-mergeable — the only variable case is `MgVar` (`LVar` with `LVar`), discharged by MERGE IDEMPOTENCE `merge(s,s)=s` on the substituted value. Induction on the mergeability derivation. So bystander projection at a choice IS stable under unfolding. Plus FULL CHOICE-UNDER-RECURSION COMPLETED (`b6c56e46`, same probe extended, Idris `rel=same`): a recursive `Global` carrying CHOICE (`GCho`) and recursion (`GRec`/`GVar`), `project` for all roles (including the bystander-merge branch), `gsubst`, and a well-formedness predicate `WF` carrying the bystander mergeability witness at each choice (`WFCho`). `project_subst_hom` proves `WF(g) ⟹ project(gsubst(g,s),r) = lsubst(project(g,r), project(s,r))` — projection is a substitution homomorphism WITH choice (the bystander-merge case chains `merge_cong` with `merge_lsubst_commute` on the `WFCho` witness via `eqv_trans`, relying on KERNEL CONVERSION rather than rewrite occurrence-finding, so it sidesteps the E12 δ-blind-rewrite gap). The capstone `unfold_commute` then gives `project(unfold(GRec body), r) = unfold_local(project(GRec body, r))` for well-formed recursive protocols WITH branching — a looping multiparty protocol with choice keeps EVERY role's projection (active AND bystander) stable across every unfolding. So the recursive multiparty story is complete: messages, recursion, duality/coherence, AND choice all commute with unfolding. Plus N-ARY LABEL SETS STARTED (new probe `otp_nary_choice`, Idris `rel=same`, enabled by the mutual-recursion δ-reduction fix `a4f071fb`): choice branches are an ARBITRARY-LENGTH list (`Branches = BNil | BCons(Tag, Local, Branches)`, mutually inductive with `Local`), not a fixed binary pair. `dual`/`dual_branches` mutually recursive; `dual_involution` (`158b07be`) proves N-ARY DUALITY IS AN INVOLUTION (LSel↔LBra + send↔receive swaps undo themselves over arbitrary-arity lists). Then `projection_duality` (`1ed73ef9`) lifts it to GLOBAL projection: a two-party protocol whose choice offers an n-ary `GBranches` list projects its two roles to DUAL n-ary endpoints (n-ary LSel dual to n-ary LBra), via FOUR mutually-recursive defs — `project`/`project_gbranches`, `Coherent`/`CoherentB`, `projection_duality`/`projection_gbranches_duality` — the Honda-Yoshida-Carbone coherence generalized binary→n-ary. Remaining n-ary: bystander MERGE over an n-ary branch list (fold merge across branches) + n-ary in the full branch-merge subtyping/operational system. Open: n-ary merge/subtyping/operational (in progress); and per-participant ASYNCHRONOUS configuration semantics (interleaved in-flight messages / mailbox buffers — requires PEER-annotated local types, since projected `LSend`/`LRecv` don't name their peer; a grammar refactor, not a lemma).
- [ ] Evolving-protocol inference (accepted set changes over a conversation) — composes B2 fixpoint + this solving

### B4. Full ordered / protocol selective receive (rest of G8)
- [x] PATTERN-DIRECTED selective receive (a pattern = a set of acceptable tags, generalizing the single-tag scan): `SelRecv(set, before, got, after)` with `selrecv_matches` (received message matches the pattern) + `selrecv_present` (it was actually in the mailbox) — **SHIPPED** `otp_pattern_receive.cure` (Idris `rel=same`). **DONE**: whole-conversation protocol-level ordering — `Otp.Meta.Conversation`: `ConvRecv` runs a `Protocol` (sequence of expected tags) over a mailbox via per-step selective receive; `conv_order` proves the received conversation equals the protocol's tag sequence regardless of the mailbox's arrival order (a protocol-directed client always receives in protocol order). Idris `rel=same`
- [x] **MULTIPARTY PROGRESS BY PROJECTION, arbitrary roles + BYSTANDER TRANSPARENCY** (`multiparty_progress.cure`): the Honda-Yoshida-Carbone progress theorem generalised beyond the fixed-RA/RB two-party probes (`multiparty_session`/`three_party`, which fix the active pair). For a communication `GMsg(p,q,t,k)` with ARBITRARY roles: `sender_ready` (`project(·,p) = LSend(t,…)`) and `receiver_ready` (`project(·,q) = LRecv(t,…)` given `role_eq(p,q)=F`), plus the new `bystander_transparent` — any third role `r` with `role_eq(p,r)=F` and `role_eq(q,r)=F` projects PAST the step (`project(GMsg(p,q,t,k),r) = project(k,r)`), the property that makes projection well-defined for true N-party sessions, by 27-way role analysis with the two off-role premises refuting the `r=p`/`r=q` diagonals (empty `match` / Idris `Refl impossible`). `progress` packages sender/receiver readiness into `tmatches(project·p, project·q)=T` — at any `p≠q` the two active roles can communicate. Deadlock-freedom by construction, for any pair of roles. Idris `rel=same`.
- [x] **MULTIPARTY SUBJECT REDUCTION — projection preserved by the global step (all roles)** (`multiparty_preservation.cure`): the PRESERVATION half of multiparty type safety, companion to the progress theorem above. `lstep` consumes a local's head action; `after_step(g, r)` is role r's local after g's head communication fires (active sender/receiver step, bystanders keep). `subject_reduction` proves that after `GMsg(p,q,t,k)` reduces to `k`, EVERY role r lands on `project(k, r)` — the whole system stays in projection-correspondence with the reduced global, for ARBITRARY `p,q,r` with NO well-formedness premise (after_step and project branch on the same `role_eq` guards, so all 27 role cases reduce reflexively). Progress + preservation = full multiparty type safety: a projected system is well-typed ⟹ never stuck ⟹ stays well-typed, any roles. Idris `rel=same`.
- [x] **OPERATIONAL FIDELITY with DIRECTED locals — the projected config follows the protocol, and only it** (`session_fidelity.cure`): the operational-correspondence theorem on a richer model — DIRECTED locals (`LSend`/`LRecv` record the PEER role), which lets 'the sender can only send to q' become expressible (the undirected `multiparty_progress` model could not). For a head `GMsg(p,q,t,k)`: `sender_forced` (the sender's projection is EXACTLY `LSend(q,t,…)` — completeness/determinism), `receiver_forced` (EXACTLY `LRecv(p,t,…)` given `p≠q`), `bystander_idle` (a third role projects past the step), `sender_lands`/`receiver_lands` (performing the head puts each active role at `project(k,·)` — soundness, via `rewrite`), and `fidelity_ab` (the concrete whole 3-role config: firing `RA→RB:t` drives `config(GMsg(RA,RB,t,k))` to `config(k)` EXACTLY, rewrite-chaining the three lemmas). The projected configuration steps in lockstep with the global protocol — forced actions upward, correct landing downward. Idris `rel=same`.
- [x] **GENERAL WHOLE-CONFIG FIDELITY (arbitrary roles)** (`session_fidelity_general.cure`): generalizes `session_fidelity`'s concrete `RA→RB` step to ANY head `p→q`. `role_after(g,p,q,r)` is role r's post-state when the head fires (sender performs send, receiver performs receive, bystander idle); `role_fidelity` proves every role r lands on `project(k,r)` with NO `p≠q` premise (when the receiver case fires, `p≠q` is already forced) — 27-way, each case reducing reflexively through `project` + `perform`; `config_fidelity` assembles the three per-role results via config congruence: firing `p→q` drives the whole configuration of `GMsg(p,q,t,k)` to `config(k)` EXACTLY, for arbitrary roles. The projected system steps in lockstep with the global protocol for every pair of roles. (`after` is a reserved Erlang atom — renamed `role_after`.) Idris `rel=same`.
- [x] **RUN-LEVEL OPERATIONAL FIDELITY — the config executes the whole protocol** (`session_fidelity_run.cure`): lifts single-step `config_fidelity` to an entire protocol. `CRun` is the reflexive-transitive closure of the REAL operational step (`step_config` firing a head `p→q`); a `CRStep` from `config(GMsg(p,q,t,k))` goes to `step_config(GMsg(p,q,t,k),p,q)` (the genuine operational result). `config_terminates` proves `config(g)` CRuns all the way to `config(GEnd)` for every `g`: at each head the operational step lands on `config(continuation)` (`config_fidelity` transported into the step target via `rewrite`), and induction runs the rest. The projected multiparty configuration executes the ENTIRE global protocol to completion, step for step — full multi-message operational correspondence. Idris `rel=same`.
- [x] **COHERENCE — the well-formedness premise licensing progress + preservation** (`multiparty_coherence.cure`): closes the MPST triple (coherence → progress → preservation). `Coherent` is the inductive predicate: `GEnd` coherent; `GMsg(p,q,t,k)` coherent when `role_eq(p,q)=F` (distinct sender/receiver, no self-send) and `k` coherent. `coherent_head_distinct` extracts the distinctness witness; `coherent_tail` proves the continuation of a coherent global is coherent (coherence PRESERVED by reduction, so subject reduction stays within coherent types); `coherent_progresses` feeds coherence's distinctness premise into `progress`, so a coherent global NEVER gets stuck (deadlock-freedom for well-formed sessions). Together with `multiparty_progress`/`multiparty_preservation` this is the full canonical MPST safety theorem over `project`/`role_eq`, arbitrary roles. FINDING: `coherent_progresses` needs `p,q,t,k` RELEVANT (explicit params), since Cure implicit binders are erased and `progress` consumes them relevantly (`:erased_used_relevantly` otherwise). Idris `rel=same`.
- [x] **GUARDEDNESS / PRODUCTIVITY for recursive global types** (`guarded_recursion.cure`): the recursion well-formedness condition the existing recursive `Coherent` (`multiparty_recursive`) is MISSING — it admits `CoRec(CoVar) : Coherent(GRec(GVar))`, the unproductive loop `X = X`. `head_guarded` flags a productive head (a message/end vs a bare recursion variable). `guarded_subst` (substitution preserves head-guardedness; GVar case vacuous) and `unfold_productive` (unfolding a head-guarded body yields a productive head, never a stuck variable — the recursive analogue of progress: a guarded loop always makes an observable step) plus discriminating witnesses `pingpong_guarded`/`pingpong_unfolds_to_comm` (X = RA→RB:TA unfolds to a real communication then loops) vs `barerec_unguarded` (X = X flagged unguarded — exactly the case recursive `Coherent` wrongly accepts). Strengthens recursive well-formedness to rule out non-productive protocols. Idris `rel=same`.
- [x] **GUARDED RECURSIVE COHERENCE — well-formedness entails productivity** (`guarded_coherence.cure`): folds guardedness INTO the coherence predicate. `GCoherent`'s `GCoRec` carries a `head_guarded(body)=T` proof, so `GCoherent(GRec(GVar))` is UNCONSTRUCTIBLE (its guard witness would need `head_guarded(GVar)=F` to equal `T`) — closing the gap where the recursive `Coherent` admitted `X = X`. `gcoherent_head_distinct` (distinct roles), `gcoherent_guard` (extract the guard witness), and `gcoherent_productive` (THE PAYOFF: every coherent recursion unfolds to a productive head, via `unfold_productive` — well-formedness now ENTAILS productivity by construction). `pingpong_gcoherent` constructs `GCoherent` for the guarded `X = RA→RB:TA. X` loop while `X = X` cannot be formed. New probe; leaves the existing `multiparty_recursive` fixture untouched. Idris `rel=same`.
- [x] **MERGEABILITY IS ORDER-INDEPENDENT (branch-merge symmetry)** (`merge_symmetry.cure`): complements `branch_merge`'s lower-bound/projectability results. The merge OUTPUT is branch-order-sensitive (differing receives merge to `LBra(a,k1,b,k2)` whose order flips on swap — so merge is NOT syntactically commutative; a finding in itself), but MERGEABILITY is not: `mergeable_sym` (`Mergeable(x,y) ⟹ Mergeable(y,x)`, matching prefixes recurse, the differing-receive case flips its witness via `tag_eq_sym`) and `merge_defined_sym` (the merge in EITHER order is well-defined, via `merge_ok`). So a bystander's projectability at a choice is symmetric — branch order doesn't affect whether projection succeeds. NOTE the full universal property (merge = GREATEST lower bound / meet) remains open — it needs hard subtyping inversions (a common lower bound of two distinct receives must be reconstructed as a branch), a genuine research proof. Idris `rel=same`.
- [x] **MERGE IS THE GREATEST LOWER BOUND — the universal property (meet)** (`merge_glb.cure`): closes the gap flagged by `merge_symmetry`. `branch_merge` proves merge is A lower bound; this proves it is the LARGEST — any common lower bound is below the merge. Done in the faithful n-ary MPST model (external choice = branch SET, width subtyping `Covers` = 'offers a superset', merge = branch UNION), where the meet law is clean set reasoning instead of the binary-positional subtyping inversions that made it intractable. `covers_glb` (a set covering both `s` and `t` covers their union `bappend s t`, by induction on the first `Covers` derivation) lifts through the `Sub` wrapper into `merge_greatest`: any common lower bound `z` of `x` and `y` satisfies `z <: merge(x,y)`. With `merge_sub_l/r` (lower bound) merge is now pinned as the MEET of the subtyping lattice — the canonical, most-permissive combination of branch projections. The universal property of branch-merge is CLOSED. Idris `rel=same`.
- [x] **MERGE LATTICE JOIN COMPLETE — the LUB; subtyping is a FULL LATTICE** (`merge_join.cure`): the branch INTERSECTION is the LEAST common supertype, dual to `merge_glb`'s meet. Both blockers cleared: membership decision-heaviness → EVIDENCE-CARRYING `mem_dec`; and a function-type negation `InB→Empty` that hit an elaborator gap on dependent higher-order params → replaced by an INDUCTIVE `NotIn` carrying `teq=OF` witnesses (`notin_absurd` refutes dropping a present element). `in_inter` matches `s` STRUCTURALLY so `inter` reduces (GADT-index refinement did NOT reduce `inter(s,t)` — the function-app-on-refined-index wall), then `covers_join_least`/`join_least` prove any common supertype is above the join. With `merge_glb` (meet) + `merge_semilattice` + `subtyping_preorder`, external-choice session subtyping is now a FULL LATTICE (meet AND join). Idris `rel=same`. FINDINGS: empty type is `type Empty = |`; function-type params with dependent indexed domains hit a conversion gap (use inductive relations instead).
- [x] **SESSION SUBTYPING ABSORPTION LAWS — the algebraic lattice** (`merge_lattice_absorb.cure`): completes the lattice ALGEBRAICALLY. With `merge_glb` (meet), `merge_join` (join) and `merge_semilattice` giving the order-theoretic lattice, the two ABSORPTION laws tie meet and join into a genuine algebraic lattice up to subtyping-equivalence: `absorb_meet` (`s ∩ (s∪t) ≈ s`) and `absorb_join` (`s ∪ (s∩t) ≈ s`). The crux `inter_super` proves `s ⊆ u ⟹ inter(s,u) ⊇ s` (every element of s survives the intersection since it is in u; the `MNo` case refuted by `notin_absurd`). `absorb_join` falls out of `covers_glb`+`covers_refl`+`covers_inter_l`; `absorb_meet` from `inter_super`+`merge_lower_l`+`covers_inter_l`. External-choice session subtyping is now a lattice order-theoretically AND algebraically. Idris `rel=same`.
- [x] **MEMBERSHIP ALGEBRA of the branch operations** (`merge_membership.cure`): union and intersection compute the set union/meet at the ELEMENT level — `in_union_l`/`in_union_r` (member of either side is in the union), `in_union_split` (member of the union came from one side, via a `MemOr` disjunction), `inter_mem_l`/`inter_mem_r` (member of the intersection is in BOTH operands) + `in_inter` (in both ⟹ in the intersection). So `InB(x, a∪b) ⟺ InB(x,a) ∨ InB(x,b)` and `InB(x, a∩b) ⟺ InB(x,a) ∧ InB(x,b)` — the semantic characterization underpinning the whole subtyping lattice, and the foundation for distributivity. FINDING: `inter_mem_l/r` cannot match `m : InB(x, inter(s,t))` (the computed-index-match wall — `inter(s,t)` won't reduce to a constructor at the witness's type); delegate to a GENERIC `covers_mem` (inducts over an ABSTRACT small) to sidestep it. Idris `rel=same`.
- [x] **LATTICE OPERATIONS ARE MONOTONE** (`merge_monotone.cure`): union (meet) and intersection (join) PRESERVE the subtyping order — what makes the lattice usable for compositional refinement. `inter_mono_r` (intersection monotone in the 2nd arg; the `x∈t1,x∉t2` case impossible via `covers_mem`+`notin_absurd`), `inter_mono_l` (monotone in the 1st arg; each kept branch of s1 lands in `inter(s2,t)` via `in_inter`), `union_mono` (union monotone in both, via `covers_glb`+`covers_trans`). With the lattice laws + membership algebra, `(Branches, ∪, ∩)` is an ORDERED lattice — underpins compositional subtyping. Both sides elaborated first try on the now-validated membership machinery. Idris `rel=same`. (Full distributivity remains open: its hard inclusion needs a 4-way mem_dec case analysis with covers_trans bridges over the shrinking-s mismatch — genuinely large/tedious, not lattice-hard.)
- [x] **SESSION SUBTYPING IS A DISTRIBUTIVE LATTICE** (`merge_distrib.cure`): `s ∩ (t ∪ u) ≈ (s ∩ t) ∪ (s ∩ u)` up to subtyping-equivalence — the strongest lattice property (distributivity fails in a general lattice, holds for the powerset/branch-set model). Forward inclusion via `inter_mono_r` + `covers_glb`; the hard BACKWARD inclusion cracked by inducting on the COVERING WITNESS (`build_back`: any C covered by s AND by t∪u is covered by (s∩t)∪(s∩u), each branch split via `in_union_split` then placed by `in_inter`+`in_union_l/r`) rather than on s — sidestepping the multi-`inter` goal-refinement wall that blocked the induction-on-s attempt (same lesson as join-leastness: the wall was the proof shape, not the mathematics). The subtyping lattice is now COMPLETE: order + meet + join + semilattice laws + absorption + monotonicity + membership algebra + DISTRIBUTIVITY. Idris `rel=same`.
- [x] **SESSION WIDTH-SUBTYPING IS A PREORDER (reflexive + transitive)** (`subtyping_preorder.cure`): complements `merge_glb` (the meet) with the subtyping order's own laws. `covers_refl` (every branch set covers itself), `covers_mem` (membership TRANSPORT — if `big` covers `small`, every branch of `small` is in `big`, the crux, by induction on the membership derivation reading the covering witness at each position), and `covers_trans` (`Covers(a,b)` ∧ `Covers(b,c)` ⟹ `Covers(a,c)` — subtyping CHAINS, what lets refinements compose, by induction on the second covering with membership transport through the first). With `merge_glb`'s union-is-the-GLB, external-choice session subtyping is now a MEET-SEMILATTICE preorder. Idris `rel=same`.
- [x] **MERGE IS A SEMILATTICE MEET (comm/idem/assoc up to subtyping-equivalence)** (`merge_semilattice.cure`): resolves the non-commutativity finding from `merge_symmetry` — syntactic equality is the wrong notion in a PREORDER. Up to `Equiv` (mutual covering, the equivalence the preorder induces), merge (branch union) satisfies every meet law: `merge_comm` (`s∪t ≈ t∪s`), `merge_idem` (`s∪s ≈ s`), `merge_assoc` (`(s∪t)∪u ≈ s∪(t∪u)`), each direction assembled purely from `covers_glb` (decompose the union) + `merge_lower` (each half covered) + `covers_trans` (re-bracket) — no stuck membership, no inversions. With `merge_glb` (union = GLB) and `subtyping_preorder` (refl+trans), external-choice session subtyping is now a genuine MEET-SEMILATTICE — the complete order-theoretic characterization. Idris `rel=same`.

### B5. Signals semantics depth (KWC parity and beyond)
- [x] PER-SENDER signal ordering (KWC's pairwise guarantee): a mailbox is an `Interleave` of per-sender streams; `proj_left`/`proj_right` prove projecting to a sender recovers exactly that sender's stream in order — each sender's signals stay FIFO regardless of the interleaving — **SHIPPED** `otp_signal_order.cure` (Idris `rel=same`). Refines the single global mailbox-FIFO model to Erlang's actual pairwise ordering.
- [x] link / monitor / exit modelled as SIGNALS with KWC ordering guarantees — SHIPPED `otp_signal_kwc.cure` (Idris `rel=same`, `0e6befba`). Heterogeneous per-sender stream `Sig = SMsg | SLink | SExit(Reason) | SMon | SDown(Reason)`; `proj_left`/`proj_right` = pairwise FIFO for signals (link/monitor ordered), `death_last_proj`/`_b` = exit/down delivered LAST per peer (`DeathLast` preserved through interleave+projection), with a concrete non-vacuous witness. All four signal kinds now have their KWC ordering: live signals via FIFO, death notifications via terminality.

### B6. Supervision depth
- [x] Restart INTENSITY bound (`max_restarts`) — `otp_restart_intensity.cure` (budget index; can't restart at zero). `max_seconds` window abstracted; bounded-run liveness theorem (`eventually_down`) now elaborates in-module (E6 unblocked). Sharpened by `eventually_down_len` (RESTART INTENSITY IS EXACT: `run_len(eventually_down(n)) = S(n)` — the canonical run tolerates EXACTLY `n+1` failures, `n` restarts then one shutdown, by induction on the budget via `snat_cong`), turning the qualitative "reaches Down" into a precise count. Idris `rel=same`
- [x] Differential restart-SET semantics `one_for_one` / `one_for_all` / `rest_for_one` tied to preservation (NVLang Def. 4.2) — all three strategies in `otp_supervisor.cure`, each preserving `Fleet(specs)`, differing only in which children they revive
- [x] Dynamic children / `simple_one_for_one`: `Pool(spec)` indexed by a SINGLE spec (not a list) certifies UNIFORM children; `start_child`/`terminate_child` vary the pool at runtime, `restart_pool` preserves spec + size — **SHIPPED** `otp_supervisor.cure` (Idris `rel=same`). The pool COUNT (not in the `Pool(spec)` index) now has a proved DYNAMIC-MEMBERSHIP ALGEBRA: `start_grows` (`pool_size(start_child(p)) = S(pool_size(p))` — grows by exactly one), `terminate_start_id` (`terminate_child(start_child(p)) = p` — the pool is a stack, push then pop cancels), and `restart_preserves_size` (`pool_size(restart_pool(p)) = pool_size(p)` — reviving every child changes liveness, not membership, by induction on the pool). So a `simple_one_for_one` supervisor's child count moves only by explicit start/terminate, never by a restart. Idris `rel=same`
- [x] Supervision restart AXES made ORTHOGONAL + self-contained in the oracle cluster (four differential probes; complement the intrinsic `Fleet(specs)`/`Pool` above with plain-data generation-count models that expose the numeric semantics directly): **(1) WHICH children — `supervision_restart.cure`**: generation-bumping strategies with `one_for_all_correct` (`AllBumped(cs, bump_all cs)` — one_for_all restarts EVERY child, inductive), `rest_for_one_head_is_all` (rest_for_one at the head coincides with one_for_all), and `one_for_one_isolates` (FAULT ISOLATION — a later child’s failure leaves an earlier sibling’s generation untouched, the property that distinguishes one_for_one; new angle vs the intrinsic Fleet version, which proves spec-preservation but not sibling isolation). **(2) HOW MANY — `restart_intensity.cure`**: a supervisor as a restart BUDGET, `supervisor_escalates` (budget `b`, `S(b)` consecutive failures ⇒ `GaveUp` — runaway restart loops impossible) + `honours_budget` (exactly `b` failures leave it `Running(Z)` — no premature give-up), pinning intensity EXACTLY (self-contained restatement of the main-branch `eventually_down_len`). **(3) TREE PROPAGATION (NEW) — `restart_escalation.cure`**: a two-level tree (inner child-supervisor under an outer supervisor) where an exhausted inner budget escalates one outer restart and RESETS the child to a fresh cap; `dead_absorbing` (death is irreversible — no zombie restarts), `inner_cycle` (one full inner-exhaustion cycle = `S(i)` crashes consumes EXACTLY one outer restart, general in all budgets — the inductive step of tree escalation), `final_cycle` (with the outer budget gone the last cycle kills the whole tree). First multi-level escalation composition. **(4) WHETHER a child restarts (NEW) — `restart_types.cure`**: the child_spec `restart` field decides restart-or-not from the exit reason; `permanent_always` (always), `temporary_never` (never), `transient_on_normal`/`transient_on_abnormal` (transient discriminates — down on a normal exit, restarted on a crash). All four Idris `rel=same`. The four axes (which / how-many / tree-propagation / whether) now fully characterize OTP supervision restart semantics.

### B7. Effects depth
- [x] Effect ALGEBRA + LAWS: OTP message ops carry no result, so effect programs form a MONOID under sequential composition (`seq` = append) — `seq_nil_l`/`seq_nil_r`/`seq_assoc` certify `(Eff, seq, ENil)` is a monoid, the reassociation/unit-simplification an effect handler relies on — **SHIPPED** `otp_eff_algebra.cure` (Idris `rel=same`). effect HANDLERS: a handler interprets the algebra and its correctness is a MONOID HOMOMORPHISM — `count_sends` (a send-counting handler into `(Nat,plus,0)`) + `count_hom` (`handle(a;b) = handle(a)+handle(b)`) — **SHIPPED** `otp_eff_algebra.cure` (Idris `rel=same`). Open: a value-returning free-monad `bind` (higher-order reduction in an index position, E10) + effect ROW/multiple-handler composition

### B8. Full gen_server / gen_statem lifecycle
- [x] Typed CALLBACK LIFECYCLE as a phase state machine — `Otp.Meta.Lifecycle`: `Callback` = typed phase transition (init `PInit→PRunning`, handle_* keep `PRunning`, stop `→PTerminated`, terminate final); `Lifecycle` = well-formed callback sequence. Proves `init_only` (init is the only first callback), `nothing_reinits` (no callback returns to `PInit`), `terminated_absorbing` + `no_resurrection` (a terminated server stays terminated). **SHIPPED** `otp_lifecycle.cure` (Idris `rel=same`). Open: `code_change` version transitions; per-callback message/reply typing threaded through (the `Otp.Meta.Proof`/`Otp.Meta.Call` layer)
- [x] `gen_statem` EVENT POSTPONING: `SStep` (handle/postpone/redeliver) with per-move conservation of the unprocessed count — `handle_progresses` (only handling advances), `postpone_conserves`/`redeliver_conserves` (deferring/redelivering relocate an event, never drop it) — **SHIPPED** `otp_gen_statem.cure` (Idris `rel=same`). *No paper types postpone; this is gen_statem's defining feature.* Now LIFTED to whole RUNS: `SRun` (reflexive-transitive closure of `SStep`) with `srun_single`/`srun_trans` (reachability is a PREORDER — a move is a run, and runs compose end-to-end), and `handle_count_trans` (HANDLED-EVENT COUNT IS A MONOID HOMOMORPHISM: `handle_count(srun_trans(r1,r2)) = handle_count(r1) + handle_count(r2)` — events handled by concatenating two trajectories sum, so `SRun`-composition ↦ `Nat`-addition mapping `SRDone ↦ 0`; induction on `r1` via `plus_assoc`). Structural route sidesteps binding the indexed step's existential counts. FINDING: within-module `trans`/`sym` need an explicit `use Std.Equivalent` for CODEGEN (elaboration resolves them ambiently, but codegen emits `:unknown_global` without the import — `reflexive`/`rewrite` are fine without it). Open: full state-function callback protocol
- [x] **GEN_STATEM SAFETY BY INVARIANT INDUCTION — mutual exclusion** (`gen_statem_invariant.cure`): the fundamental behavioural-verification pattern. A gen_statem lock has state = held-count, events acquire/release; the safety invariant is MUTUAL EXCLUSION (`count ≤ 1`). `inv_step` proves every transition preserves it (the subtle case — release from a held lock — is forced to count 1 by the invariant); `inv_reachable` lifts it to ALL reachable states by induction on the `Run` (applying `inv_step` at each event); `mutex_safe` instantiates at the free start, so no reachable configuration ever has two holders, whatever the event sequence. Core soundness of invariant-based verification in Cure, distinct from the postpone/event-count `gen_statem` work. Idris `rel=same`.
- [x] **FORWARD SIMULATION / DATA REFINEMENT** (`gen_statem_refinement.cure`): a concrete gen_statem (lock tracking a held-COUNT) faithfully implements an abstract spec machine (named states AFree/AHeld). The refinement relation `R` abstracts the count (0 ~ AFree, 1 ~ AHeld). `sim_step` proves every concrete transition is matched by the abstract one preserving `R`; `sim_run` lifts it to whole event sequences (running both machines on the SAME events keeps them R-related); `refines` instantiates at the aligned start — the concrete lock is correct BY CONSTRUCTION against the spec. The basis of stepwise refinement, distinct from invariant induction and from session fidelity (this is abstract↔concrete DATA refinement). Idris `rel=same`.
- [x] **SAFETY VIA REFINEMENT — property transfer through the simulation** (`gen_statem_transfer.cure`): the payoff of forward simulation, tying invariant-induction and refinement together. Instead of re-proving mutual exclusion on the concrete lock by a fresh invariant induction, it is TRANSFERRED through the refinement relation `R`: `r_bounds` shows `R` carries the count bound (any R-related concrete state has `count ≤ 1`), and `sim_run` preserves `R` over every run, so `mutex_by_refinement` gives `count ≤ 1` in ALL reachable concrete states with no fresh concrete invariant. Verify the simple abstract, get the concrete property for free — the reason stepwise refinement is used. Idris `rel=same`.
- [x] **LIVENESS BY A WELL-FOUNDED MEASURE — a bounded retry loop always terminates** (`retry_liveness.cure`): the verification category beyond safety and refinement (eventual progress). A retry FSM has bounded attempts (`Retry n → … → GaveUp`); the measure `attempts+1` STRICTLY DECREASES on every non-terminal step (`measure_decreases`, terminal cases refuted by the non-terminal hypothesis) — the well-foundedness that forces termination. `reaches_terminal` proves the loop lands on a terminal state within `measure(Retry n) = n+1` steps, by induction on the attempt count. No infinite retrying: OTP's `max_retries` as a liveness guarantee. Completes the state-machine verification trio (safety / refinement / liveness). Idris `rel=same`.
- [x] **COMPOSITIONAL SAFETY — concurrent mutual exclusion** (`conc_mutex.cure`): the first PARALLEL-composition result. A `Config` holds both processes' states (Idle/Crit); a `Move` advances ONE process (interleaving); entry is GUARDED on the peer not being critical. `mutex_step` proves every interleaved move preserves the joint invariant 'not both critical' (the violating case — entering while the peer is critical — refuted by the invariant hypothesis); `mutex_reachable` lifts it over any interleaved run; `mutex_safe` gives mutual exclusion in ALL reachable configurations from the idle start, whatever the schedule. Concurrent safety, distinct from the sequential single-machine trio (invariant / refinement / liveness). Idris `rel=same`.
- [x] **COMPOSITIONAL DEADLOCK-FREEDOM — the concurrent mutex protocol never wedges** (`conc_deadlock_free.cure`): companion to concurrent mutual exclusion. From EVERY configuration some process can make progress — even though entry is guarded (a process blocks while the peer is critical), a critical process can always EXIT. `pick` chooses an enabling move per config; `deadlock_free` proves `same(step(c, pick(c)), c) = F` (the picked move yields a genuinely different config), so the system is never stuck, whatever the state (universal over all configs, a fortiori the reachable ones). The concurrent progress guarantee alongside the concurrent safety guarantee. Idris `rel=same`.
- [x] **CONCURRENT LIVENESS UNDER FAIRNESS — fair enters, unfair starves** (`fair_liveness.cure`): the honestly-hard concurrent property, FALSE without a fairness assumption. BOTH directions: POSITIVE `fair_enters` — a `Fair` schedule (one that eventually lets P1's guarded entry fire) drives the waiting process to the critical section (`EvP1Crit`), by induction on the fairness witness; NEGATIVE `starved` — a `P2Only` (unfair, peer-only) schedule keeps P1 idle indefinitely (each peer move preserves idle via `enter2`/`exit2_keeps_idle`), so P1 never enters. The negative proves the fairness hypothesis is ESSENTIAL, not vacuous — fairness is exactly what liveness needs. Completes the concurrent layer (safety / deadlock-freedom / liveness-under-fairness). Idris `rel=same`.
- [x] **N-PROCESS MUTUAL EXCLUSION — arbitrary process count** (`n_mutex.cure`): scales concurrent mutual exclusion from a fixed pair to an arbitrary number of processes (a LIST). The joint invariant `AtMostOne` is a global inductive predicate over the list; entry is GUARDED by an evidence-carrying 'all idle' decision (`check_idle`/`IdleDec`, routed through `acquire_step` so the proof refines — the same helper trick as `inter_pick`); release frees everyone. `acquire_amo` (entering from all-idle yields exactly one critical), `step_amo` (every guarded operation preserves `AtMostOne`), `mutex_safe` (lifts to all reachable states from an all-idle start). Mutual exclusion for ANY number of processes and any schedule. Idris `rel=same`.
- [x] **N-PROCESS DEADLOCK-FREEDOM — never wedges at any scale** (`n_deadlock_free.cure`): progress companion to N-process mutual exclusion. From any NON-EMPTY configuration the move enabled by the current all-idle status FLIPS it — `all_idle_b(stepped(c, all_idle_b(c))) = neg(all_idle_b(c))`. An all-idle system acquires (`T→F`, head goes critical); a busy one releases (`F→T`, everyone frees, via `release_all_idle`). Routed through `stepped(procs, b)` so the decision is an explicit matched argument (the helper trick). The N-process system is never stuck, any count, any state. With `n_mutex` this gives both concurrent guarantees at arbitrary scale. Idris `rel=same`.
- [x] **ROUND-ROBIN FAIRNESS FOR N PROCESSES** (`fair_roundrobin.cure`): lifts fair scheduling to arbitrary process count. The scheduler is a rotating queue (head = scheduled process, `rotate` moves it to the back). Three fairness properties: `rotate_advances` (rotating a ≥2 queue brings the SECOND to the head — the turn walks toward any waiter), `rotate_length` (the process COUNT is preserved via `len_snoc`), `rotate_mem` (MEMBERSHIP preserved — no process ever dropped from the rotation, so no starvation by omission; head→back via `in_snoc_last`, rest stays via `in_snoc`). Every process keeps its slot in the cycle and is reached in turn. Completes the concurrent trio at N-scale (safety `n_mutex` / deadlock-freedom `n_deadlock_free` / fairness). Idris `rel=same`.
- [x] `gen_event` EVENT MANAGER (`Otp.Meta.GenEvent`, a new OTP behaviour): a `Manager(ifaces)` INDEXED by its handlers' interface list; `notify(e)` BROADCASTS to every handler (each processes via `step` if the event is in its interface, else ignores). The characteristic guarantee = a broadcast preserves the handler CONFIGURATION: `notify : Manager(ifaces) -> Manager(ifaces)` (type-level), plus explicit `notify_preserves_count` (handler count) and `notify_preserves_ifaces` (exact interface list) — so the manager's shape is stable under the event stream, only states advance. `add_handler` extends the interface index. The per-handler `step` helper keeps `notify` reducing STRUCTURALLY, sidestepping the E8 sequential-match gap. Plus DYNAMIC HANDLER MANAGEMENT: `remove_head : Manager(ICons(iface,rest)) -> Manager(rest)` (type-certifies the interface list SHRINKS), `add_remove_inverse` (`remove_head(add_handler(...)) = mgr` — install then remove restores the manager exactly, so a transient handler leaves no trace) and `count_after_add` (installing increments the count). Plus REMOVE-AT-POSITION (`Pos(ifaces)` witness `Here`/`There` + `delete_at` + `remove_at`): a DEPENDENTLY-TYPED list deletion — `remove_at(mgr, pos) : Manager(delete_at(pos))` removes exactly the handler at `pos` (all others, before and after, preserved in order), with the `Pos` witness guaranteeing the position exists so removal is total and can't drop the wrong handler. Recurses `match evidence(pos); match data(mgr)` (E1 sibling-refinement convoy). Plus SWAP/CODE_CHANGE (`swap_at` + `swap_preserves_ifaces`): replacing a handler's implementation (state) at `pos` keeps its interface slot — typed `Manager(ifaces) -> Manager(ifaces)` (same interface list, type-certified) with an explicit `swap_preserves_ifaces` confirming a code_change leaves the manager's configuration unchanged. **SHIPPED** `otp_gen_event.cure` (Idris `rel=same`). The event-manager behaviour is now complete: broadcast, install/remove, remove-at-position, swap/code_change.
- [x] `ETS` typed key/value table (`Otp.Meta.Ets`, the BEAM storage primitive): a `set`-semantics table as an association list; `lookup`/`insert` proved a real finite map — `lookup_insert_eq` (LOOKUP-AFTER-INSERT: `lookup(k, insert(k,v,t)) = OSome(v)`) and `lookup_insert_neq` (a write to a DIFFERENT key is invisible: `key_eq(k,k2)=F ⟹ lookup(k, insert(k2,v,t)) = lookup(k,t)`, impossible same-key diagonals refuted via an `Empty` discriminator `t_ne_f`/`absurd`). NOTE: because `key_eq` is stuck on an abstract key, the K1 freeze keeps `lookup` FOLDED, so the laws are proved by CASE ANALYSIS on concrete keys (which makes `key_eq` compute) rather than `rewrite` — a real interaction of the K1 fix with decidable-equality reasoning. Plus `lookup_delete` (LOOKUP-AFTER-DELETE: `lookup(k, delete(k,t)) = ONone` — looking up a deleted key finds nothing; induction on the table casing on both keys so `key_eq` computes, at a matching head `delete` drops it, at a non-matching head `lookup` skips it, both continue to the deleted tail by IH). All three fundamental finite-map laws now certified. **SHIPPED** `otp_ets.cure` (Idris `rel=same`). Open: ordered_set semantics.
- [x] ETS `bag` / `duplicate_bag` MULTISET table (`Otp.Meta.Bag`): distinct from the `set` table (overwrite) and the ordered map — a `duplicate_bag` KEEPS every insertion, so the meaningful observation is a key's MULTIPLICITY `count(k, t)`. The COUNT ALGEBRA is certified: `insert_incr` (INSERT INCREMENTS: `count(k, insert(k,t)) = S(count(k,t))` — a duplicate_bag adds an occurrence, never overwrites), `insert_other` (a DIFFERENT key's count is untouched: `keq(x,k)=MF ⟹ count(x, insert(k,t)) = count(x,t)`, same-key diagonals refuted by the premise `neq : MT = MF`), and `delete_insert` (DELETE-ONE UNDOES INSERT: `delete_one(k, insert(k,t)) = t` — `ets:delete_object` of a just-added occurrence returns the original bag; insert/delete_one are a push/pop pair). Finite key domain so `keq` reduces at proof time. Plus `count_delete_one` (DELETE-ONE DECREMENTS, saturating: `count(k, delete_one(k,t)) = mpred(count(k,t))` — removing one occurrence drops the count by one, clamped at zero if absent; induction on the bag, at a matching head the `S(...)` cancels the `mpred`, else the tail carries by IH), so with `insert_incr` the multiplicity is fully pinned under both ops (`+1` / `−1`-clamped). **SHIPPED** `otp_bag.cure` (Idris `rel=same`). Open: `ordered_set`.
- [x] Erlang `:queue` amortized TWO-LIST functional queue (`Otp.Meta.Queue`, the FIFO primitive OTP uses everywhere): a `MkQ(front, back)` with logical sequence `front ++ reverse(back)`; the non-unique representation's correctness is that operations preserve the logical sequence. `enqueue_appends` (ENQUEUE APPENDS: `to_list(enqueue(x,q)) = to_list(q) ++ [x]` — pushing onto the reversed back lands `x` at the END of the logical order, via definitional `reverse(QCons(x,b)) = reverse(b) ++ [x]` + `append_assoc`), so the two-list encoding is a FAITHFUL FIFO. List ctors named `QNil`/`QCons` to avoid ambient list-ctor name collision when a using-module restates the type. Plus `dequeue` + `dequeue_reassembles` (DEQUEUE SPLITS OFF THE HEAD, losing nothing: `reassemble(dequeue(q)) = to_list(q)` in BOTH the fast non-empty-front case and the amortized front-empty back-REVERSED case — so the amortization is correct, reversing the back to refill the front preserves the FIFO sequence; reversal case reuses `deq_list_reassembles` + `append_nil_r`). TWO K1/ordering findings: (a) `dequeue`'s front-empty case must be LITERALLY `deq_list(reverse(b))` (not an inlined `match reverse(b)`) so it and the proof freeze to the SAME folded head; (b) `deq_list` must be defined BEFORE `dequeue` — certification is IN-ORDER, so a function calling a later-defined one isn't certified and won't δ-reduce in proofs. Plus REPRESENTATION INDEPENDENCE (`enqueue_respects`/`dequeue_respects`): the two-list split is not unique, and the operations are well-defined on the LOGICAL sequence — same `to_list` ⟹ `enqueue`/`dequeue` agree observably (enqueue via `enqueue_appends` + `append_cong`, dequeue via `dequeue_reassembles`). So the amortized queue faithfully implements the abstract FIFO; the internal split is invisible to a client. The two FIFO OBSERVATIONS are now proved too: `peek_dequeue` (PEEK AGREES WITH DEQUEUE: `peek(q) = head_of(dequeue(q))` — `peek` mirrors dequeue's front/back split so the fast case is reflexive and the front-empty case reduces to `peek_list(reverse(b)) = head_of(deq_list(reverse(b)))`), and `is_empty_reflects` (IS_EMPTY REFLECTS THE LOGICAL SEQUENCE: the O(1) both-lists-empty test `andb(qnull(f), qnull(b))` agrees with `qnull(to_list(q))` even for `{[], [a]}`-style states where the front is empty but the back is not — there both sides are `qnull(reverse(b)) = qnull(b)` via the reverse-invariance lemma `qnull_reverse`, whose cons case is discharged by `qnull_append_cons` since a `reverse` always ends in appending a singleton). **SHIPPED** (Idris `rel=same`). Open: ordered variants (priority queue).
- [x] Binary TREE backing `gb_trees`/`gb_sets` (`Otp.Meta.Tree`): STRUCTURAL laws needing no key comparison — `mirror` (swap every node's left/right subtrees) is an INVOLUTION (`mirror_involution`: `mirror(mirror(t)) = t`, via `node_cong`) and mirroring PRESERVES the node count (`size_mirror`: `size(mirror(t)) = size(t)`, via `add_comm` since mirror swaps the two subtrees so their sizes add in the opposite order). Plus IN-ORDER FLATTEN + `flatten_mirror` (MIRROR REVERSES THE IN-ORDER TRAVERSAL: `flatten(mirror(t)) = reverse(flatten(t))` — reflecting the tree walks its nodes in reverse; the node case re-associates via `lrev_app` (reverse is an append-antihomomorphism) + `lapp_assoc`). FINDING: `trans`/`cong` chains fail on definitional reductions because implicit UNIFICATION does not δ-reduce (unlike full conv), so each such step needs an explicit reflexive reduction-bridge lemma (`lrev_cons`/`lapp_cons`) to make the chain's middle terms unify syntactically. Plus `size_flatten` (SIZE = TRAVERSAL LENGTH: `size(t) = length(flatten(t))` — `flatten` visits every node exactly once, so the structural count and the traversal length agree; node case via `llen_app` distributing length over `++` + `add_succ_r`). **SHIPPED** `otp_tree.cure` (Idris `rel=same`). The ORDERED (search-tree) side is now covered by the separate `Otp.Meta.Bst` module below.
- [x] `:sets`/`:ordsets` set library (`Otp.Meta.Set`): a set as a key list, `union` as concatenation; `union_member` proves the DEFINING law of set union — MEMBERSHIP DISTRIBUTES OVER UNION as boolean OR: `member(x, union(s1,s2)) = member(x,s1) or member(x,s2)` (an element is in the union exactly when in one part). Induction on `s1` with 3×3 concrete-key casing so `key_eq` computes (diagonal ⟹ both `T` via `orb(T,_)=T`, off-diagonal ⟹ IH). NB `bor` is a reserved bitwise-or token — the boolean OR is `orb`. **SHIPPED** `otp_set.cure` (Idris `rel=same`). The union algebra is now proved at the membership level: `orb_comm`/`orb_idem` (boolean OR is commutative and idempotent) lift through `union_member` into `union_comm_member` (`member(x, union(s1,s2)) = member(x, union(s2,s1))` — operand order is irrelevant) and `union_idem_member` (`member(x, union(s,s)) = member(x,s)` — uniting with self adds nothing), both via `trans`-chaining the distribution law on both sides. Associativity is now proved too: `orb_assoc` (with per-argument congruences `orb_cong_l`/`orb_cong_r`) lifts through `union_member` on both nestings into `union_assoc_member` (`member(x, union(union(s1,s2),s3)) = member(x, union(s1, union(s2,s3)))` — regrouping is irrelevant), so union is a commutative-idempotent-associative membership algebra. The `⊆`-order is now witnessed too: boolean implication `implb` with absorption lemmas `implb_orb_l`/`implb_orb_r` gives `union_upper_l`/`union_upper_r` (`x ∈ s1 ⟹ x ∈ union(s1,s2)` and `x ∈ s2 ⟹ x ∈ union(s1,s2)`) — `union` is the join of the membership semilattice. **SHIPPED** (Idris `rel=same`). `Otp.Meta.Set` membership algebra COMPLETE (comm/idem/assoc/upper-bound).
- [x] ORDERED binary search tree backing `gb_trees`/`gb_sets` (`Otp.Meta.Bst`): keys from a fixed finite ordered domain `KA < KB < KC` with a total `kcmp`, so the comparison REDUCES at proof time (same concrete-domain technique as `Otp.Meta.Set`). `member` and `insert` both STEER BY `kcmp(k, v)` — go left on `KLt`, hit on `KEq`, go right on `KGt` — which is exactly what preserves the search invariant (left subtree < node < right). `insert_member` proves the DEFINING SEARCH-TREE LAW: `member(k, insert(k, t)) = T` — inserting a key makes a subsequent search for it succeed, whatever the tree shape. Because `member` and `insert` take the same branch at every node, casing `k` and each node key `v` (9-way) makes both reduce in lockstep: on a hit both stop at `T`, on `KLt`/`KGt` both descend the SAME subtree so the induction hypothesis carries. Plus `member_leaf` (empty tree has no members). FINDING: Idris rejected the first cut purely on PRELUDE NAME CLASHES — `Ordering`/`compare`/`LT`/`EQ`/`GT` are all in Idris's Prelude, so the transliteration must use clash-free names (`Cmp`/`kcmp`/`KLt`/`KEq`/`KGt`); this is a benign oracle-transliteration artifact, NOT a soundness divergence (Cure has no such clash). `insert_preserves` now proves INSERT PRESERVES EVERY OTHER MEMBER: `member(x,t)=T ⟹ member(x,insert(k,t))=T` — a search-tree insert never evicts a key already present. `insert` keeps the root key `v` at every node (rebuilding only one subtree), so `member(x, insert(k,t))` steers by the SAME `kcmp(x,v)` as `member(x,t)`: hit ⟹ `T`; else `x` descends its fixed side and either `k` went the same way (recurse — IH) or `k` touched the other side/root so that subtree is untouched (premise carries verbatim). 27-way casing on `x`/`v`/`k` makes every comparison reduce; the `Leaf` base is `member(x,Leaf)=F=T`, uninhabited (empty `match e` in Cure / `impossible` clause in Idris). **SHIPPED** `otp_bst.cure` (Idris `rel=same`). CAPSTONE — `insert_spec` proves the COMPLETE characterization: `member(x, insert(k, t)) = keq(x,k) or member(x, t)` — after inserting `k`, `x` is a member iff `x` IS `k` or `x` was already there. This SUBSUMES `insert_member` (`x = k`) and `insert_preserves` (`member(x,t) = T`) and also settles the last case (`x ≠ k`, `x ∉ t` ⟹ still absent). Same 27-way `x`/`v`/`k` casing: a hit ⟹ `T`; an off-side descent leaves that subtree and `keq(x,k)=F` so `orb` is the identity (reflexive); a same-side descent is the IH (defeq absorbs the `orb`). So membership-after-insert is fully pinned down. Plus `insert_comm` (INSERTS COMMUTE at the membership level: `member(x, insert(j, insert(k, t))) = member(x, insert(k, insert(j, t)))` — two keys inserted in either order give the SAME membership set though the trees differ structurally), derived ALGEBRAICALLY from `insert_spec` (each side unfolds to `keq(x,j) or (keq(x,k) or member(x,t))` via `orb_cong_r`, then the guards are swapped by `orb_swap`) — NO tree casing, a pure consequence of "insert adds exactly one key". `Otp.Meta.Bst` search-tree insert theory COMPLETE. Open: in-order flatten sortedness, delete.
- [x] `gb_trees` AS A MAP (`Otp.Meta.Gbt`): the ordered key→VALUE tree behind `gb_trees:enter`/`lookup` — distinct from `Otp.Meta.Bst` (a set) in that every node carries a value, `insert` is `enter` (insert-or-UPDATE, replacing the value on a hit), and `lookup` returns an optional value. Keys are the fixed finite ordered domain `GA < GB < GC` with total `gcmp` (concrete-domain technique, so comparisons reduce at proof time). The two DEFINING FINITE-MAP LAWS are certified: `lookup_insert_eq` (LOOKUP AFTER ENTER RETURNS THE ENTERED VALUE: `lookup(k, insert(k, val, t)) = Some(val)` — `lookup`/`insert` steer by the same `gcmp(k,v)` so they descend together and `insert` writes `val` at exactly the node `lookup` reads; 9-way `k`/`v` casing, IH on the descended subtree) and `lookup_insert_neq` (ENTERING A DIFFERENT KEY DOESN'T DISTURB A LOOKUP: given `gkeq(x,k)=BF`, `lookup(x, insert(k, val, t)) = lookup(x, t)` — since `x≠k`, `x`'s search takes either the untouched side (reflexive) or the same side `k` did (IH); every same-key diagonal is refuted by the premise `neq : BT = BF` via empty `match neq` in Cure / a `btNeBf : BT=BF -> Void` void-clause in Idris; 27-way `x`/`v`/`k` casing). Distinctive ctor names (`GKey`/`GTree`/`GLeaf`/`GNode`/`GOpt`/`GNone`/`GSome`) so the module stays unambiguous when a using-module restates types alongside the other preloaded tree/opt modules. **SHIPPED** `otp_gbt.cure` (Idris `rel=same`). CAPSTONE — `lookup_spec` proves the COMPLETE conditional law: `lookup(x, insert(k, val, t)) = if x = k then Some(val) else lookup(x, t)` (via a `gsel` selector on `gkeq(x,k)`). One statement SUBSUMING both `lookup_insert_eq` (`x = k` ⟹ guard `BT`) and `lookup_insert_neq` (`x ≠ k` ⟹ guard `BF`), needing NO premise — the same-key case now RETURNS `Some(val)` instead of being refuted. Same 27-way casing: `x = v` hits the root (`Some(val)` if `k` also hit, else the untouched `Some(w)`), an off-side descent leaves that subtree with guard `BF` so `gsel` picks the plain lookup (reflexive), a same-side descent is the IH (defeq absorbs the `gsel`). `Otp.Meta.Gbt` map lookup-after-enter theory COMPLETE. Open: delete, ordered fold.
- [x] THE DECIDABLE TOTAL ORDER ON KEYS (`Otp.Meta.KeyOrder`): the ordering every ordered structure above STEERS BY but none of them certifies. `le` is a decidable relation on the finite domain `OA < OB < OC`, and the FOUR TOTAL-ORDER AXIOMS are proved — `le_refl` (`a ≤ a`), `le_total` (`a ≤ b or b ≤ a` = `OT`, any two keys comparable), `le_antisym` (`a ≤ b` and `b ≤ a` ⟹ `a = b`; off-diagonal one direction is `OF` so its assumed-`OT` premise is uninhabited), and `le_trans` (`a ≤ b`, `b ≤ c` ⟹ `a ≤ c`; 27-way casing, when `a ≤ c` holds it computes to `OT` else whichever hypothesis is actually `OF` refutes). So the search-tree and sorted-list invariants downstream rest on a CERTIFIED order, not an assumed one. Refutations via the uninhabited `OF = OT` (empty `match` in Cure / an `ofNeOt : OF=OT -> Void` void-clause in Idris). **SHIPPED** `otp_key_order.cure` (Idris `rel=same`). Plus `kmin`/`kmax` (MEET/JOIN = `gb_sets:smallest`/`largest`, the priority-queue ordering primitive): `kmin_comm`/`kmax_comm` (commutative), `kmin_idem`/`kmax_idem` (idempotent), `kmin_lower_l`/`kmin_lower_r` (meet is a LOWER bound of both operands — why `kmin` is the minimum a PQ extracts) + `kmax_upper_l`/`kmax_upper_r` (join is an UPPER bound), and `kmin_assoc` (meet is associative), so `(OKey, kmin)` is a bounded MEET-SEMILATTICE with `kmax` the dual join. Idris `rel=same`. Open: connect to `Bst`/`Gbt` comparison, `kmax_assoc`.
- [x] **A TOTAL ORDER INDUCES A DISTRIBUTIVE LATTICE** (`key_lattice.cure`): closes the `KeyOrder` `kmax_assoc` open item and completes the algebra — the certified order (`OA<OB<OC`) with `kmin`=meet, `kmax`=join is a full DISTRIBUTIVE lattice. `kmax_assoc` (join associativity), `kmin_absorb`/`kmax_absorb` (the two absorption laws tying meet and join into a lattice), and `kmin_distrib_kmax`/`kmax_distrib_kmin` (distributivity each way — the property that fails in a general lattice but always holds for a chain). Completes the order-theoretic structure underlying every ordered structure here (Bst/Gbt/PQueue); all five laws verified combinatorially before emitting. Idris `rel=same`.
- [x] MIN PRIORITY QUEUE over the certified order (`Otp.Meta.PQueue`): Erlang has no built-in PQ (built by hand from pairing heaps / `gb_sets` / sorted lists); this models the simplest correct one — an unsorted key bag with O(1) `insert` and the minimum computed by folding the certified meet `kmin`. The CORRECTNESS THEOREM is that the extracted `pq_min` is a genuine LOWER BOUND: `min_le_seed` (`pq_min(seed,q) ≤ seed` — induction on the queue, `kmin(k,m) ≤ m` by `kmin_lower_r` then `m ≤ seed` by IH, chained by TRANSITIVITY `le_trans`) and `insert_min` (`pq_min(seed, insert(k,q)) ≤ k` via `kmin_lower_l`). SIGNIFICANCE: the FIRST structure whose safety proof CONSUMES the order/semilattice axioms (`le_refl`/`le_trans`/`kmin_lower`) rather than re-establishing them — the lib module does this by cross-module `use Otp.Meta.KeyOrder` (explicit-arg cross-module calls resolve cleanly). **SHIPPED** `otp_pqueue.cure` (Idris `rel=same`). Open: extract-min removes the minimum, `member`-level "≤ every element".
- [x] **MIN-PQ member-level correctness — the minimum is ≤ EVERY element** (`pqueue_min.cure`): closes the `pqueue` open item (it proved only `≤` the seed sentinel). `InQ` is queue membership (tail carried explicitly so the proof can name it); `min_le_member` proves that for any `x` actually in the queue, `pq_min(seed,q) ≤ x` — the defining correctness of a min-priority-queue. Induction on the membership witness: at the head the fold's `kmin` puts `pq_min` below `x` directly (`kmin_lower_l`); in the tail `pq_min ≤` the sub-min (`kmin_lower_r`) `≤ x` (IH), chained by `le_trans`. First structure whose MEMBER-level correctness consumes the certified total order. With `min_le_seed`, `pq_min` is pinned as a genuine lower bound of the whole queue. Idris `rel=same`.
- [x] **SEARCH-TREE SEARCH SOUNDNESS** (`Otp.Meta.BstSearch`, the HARD item — foundation for certified `delete`): on a WELL-FORMED binary search tree, binary search agrees with linear scan — `mem_eq_lmem` proves `is_bst(t)=OT ⟹ mem(x,t) = lmem(x,t)`, i.e. steering by the key comparison never MISSES an element a full scan finds (the search invariant is SOUND). EXTRINSIC formalization (plain `Tree` + boolean `is_bst`/`alllt`/`allgt`) after the INTRINSIC (GADT-indexed well-scoped BST) route hit a cluster of E-layer walls (see [[wellscoped-bst-elaborator-wall]]). Proof rests on the STRICT ORDER: `strict_trans` (+`strict_neq`/`strict_neq_r`) drive `below_not_lmem`/`above_not_lmem` (a key strictly below/above a bound is NOT linearly present in a subtree bounded that way), and the crux does 9-way `x`/`v` casing with the induction hypothesis on subtrees. ELABORATOR ROUTE-AROUNDS THAT MADE IT GREEN (all untrusted, NO TCB change): (a) `unfold_bst` return-position bridge to coerce `isbst(Node l v r)=OT` ↝ the `andb(…)` form (the `isbst(Node)→andb` ι-reduction does NOT fire in argument position — constructor-arg conversion gap); (b) explicit reflexive reduction-bridges `ml_*`/`ll_*` so the `trans`-chain endpoints are the reduced `mem`/`lmem` forms (`trans`/`sym` unification does not δ-reduce — same finding as the tree module's `lrev_cons`); (c) per-cell fact extraction INSIDE each `v`-branch (v concrete) — matching `v` refines `bst`'s type so the extraction type-checks, but the facts must be extracted where v is concrete, not hoisted above `match v`. Idris needs NONE of these (complete conversion) but DOES need refuting variables concrete (`void (ofNeOt p)` requires casing `y` so `cmp OC y` reduces). **SHIPPED** `otp_bst_search.cure` (Idris `rel=same`). CERTIFIED DELETE — `delete_bst` proves BST `delete` PRESERVES THE SEARCH INVARIANT: `is_bst(t)=OT ⟹ is_bst(delete(k,t))=OT`, INCLUDING the hard two-child merge/successor case. `delete` removes a node and (on a hit) MERGES its two subtrees; `merge_bst` proves the merge preserves the invariant across the deleted key's pivot — the crux being that the right subtree's lower bound must be pushed down to EACH left node, via `allgt_weaken`/`alllt_weaken` (a key strictly below/above a pivot stays below/above a weaker bound, by `strict_trans`). `delete_alllt`/`delete_allgt` prove delete preserves the node bounds; `andb_intro` assembles the three-way `is_bst` conjunction. So the order invariant is threaded through the GADT-free extrinsic tree at every node — the merge case the intrinsic route walled on. NOTE: the earlier `unify` delta-open fix (`cb2cbff1`) let `merge_bst`'s `isbst(Node)→andb` extraction work WITHOUT the `unfold_bst` bridge — the E-fix paying off in a real proof. **SHIPPED** `otp_bst_search.cure` (Idris `rel=same`). Plus DELETE REMOVES THE KEY — `mem_delete_eq` proves `is_bst(t)=OT ⟹ mem(k, delete(k,t)) = OF` (a deleted key is unfindable, whatever the tree). The hard case is `k = v` (delete = `merge(l,r)`): the chain is `mem(v, merge(l,r)) = lmem(v, merge(l,r))` (search soundness `mem_eq_lmem`, needing `is_bst(merge(l,r))` from `merge_bst`) `= lmem(v,l) or lmem(v,r)` (`lmem_merge`: merge preserves elements as a UNION, by `orb`-associativity) `= OF or OF = OF` (the deleted `v` is in NEITHER subtree: `alllt_absent`/`allgt_absent` — a key equal to a strict bound of a subtree is absent from it). So `delete` provably removes exactly its target, merge case included. Plus DELETE PRESERVES OTHER KEYS — `mem_delete_neq` proves `x≠k ⟹ mem(x, delete(k,t)) = mem(x,t)` (deleting one key leaves every other key's findability unchanged). Cleanly via the crux: reduces to a purely LINEAR `lmem_delete_neq` (needs NO `is_bst` — the `keq(x,k)=OF` premise cancels the removed node's `keq(x,v)` in the merge case, `lmem_merge` handles the union), sandwiched by `mem_eq_lmem` on both sides. So BST DELETE IS FULLY CERTIFIED: `delete_bst` (invariant preserved), `mem_delete_eq` (target removed), `mem_delete_neq` (others preserved) — the complete correctness spec, merge/successor case and all, threaded through the extrinsic order invariant. **SHIPPED** `otp_bst_search.cure` (Idris `rel=same`). Plus IN-ORDER FLATTEN SORTEDNESS — `flatten_sorted` proves `is_bst(t)=OT ⟹ sorted(flatten(t))=OT`: the in-order traversal of a BST is a STRICTLY SORTED list (the classic BST↔sorted-sequence correspondence). `flatten(Node(l,v,r)) = flatten(l) ++ v :: flatten(r)`; the crux `sorted_concat` glues two sorted halves across the pivot `v` (needs `all_lt_list(flatten l, v)`/`all_gt_list(flatten r, v)` from `flatten_all_lt_list`/`flatten_all_gt_list`, and `all_gt_list_weaken` to push a bound down each element by `strict_trans`). NOTE: unlike the search/delete proofs this needs NO 9-way key casing — there is no query key, only the tree/list structure — so it went through first try, a clean equational proof. `Otp.Meta.BstSearch` is now the COMPLETE certified BST (search soundness + full delete correctness + flatten-sortedness). Item (1) of the hard-metatheory directive DONE. **SHIPPED** `otp_bst_search.cure` (Idris `rel=same`). Open (item 2): full N-role multiparty session coherence with branch-MERGE.

### B10. Cross-pillar bridges — supervision × sessions (NEW)
These CONNECT the two pillars (fault tolerance + session types) rather than deepening either alone — the substantive frontier once each pillar is individually dense. Each a self-contained oracle probe, Idris `rel=same`.
- [x] **Session integrity across restart demands `one_for_all`** (`session_restart.cure`): a session-typed worker's config is well-typed iff worker/peer are DUAL, and a crash resets a restarted process to its INITIAL protocol state. `one_for_all_restores` proves `compatible(pr, dual(pr)) = True` for EVERY protocol (resetting BOTH participants to a protocol and its dual re-establishes the session, by induction with per-message casing so `meq` reduces). `worker_only_breaks` is the concrete counter-witness: a `one_for_one` restart mid-session leaves the worker at init `LSend(MReq,…)` while the peer is still mid-protocol at `LSend(MAns,LEnd)` — both want to SEND, so `compatible = False`. So a session-carrying worker must be supervised `one_for_all` with its peer, or a restart desyncs the protocol.
- [x] **SESSION ↔ CONCURRENCY BRIDGE — dual session processes are deadlock-free** (`session_deadlock_free.cure`): connects session-type duality (MPST work) to the concurrent progress guarantee (verification work). When endpoints `p1,p2` are DUAL, every send meets a matching receive. `dual_sound` (any process composed with `dual(its type)` is compatible), `progress` (a dual pair is terminated OR a communication redex — NEVER STUCK), `no_deadlock` (a dual pair COMMUNICATES step-by-step to completion, both `LEnd` — deadlock-free termination, by induction on the duality witness). A well-typed session pair CANNOT deadlock: the protocol's duality IS the concurrent liveness. Ties the session/MPST and behavioural-verification bodies together. Idris `rel=same`.
- [x] **INSERTION SORT PRODUCES SORTED OUTPUT — algorithm verification** (`insertion_sort.cure`): a fresh domain (verified sorting). Keys are Nat under a total order; sortedness is an inductive predicate (each head ≤ all that follow). Composes several interlocking inductions: `le_trans` (order transitivity), evidence-carrying `cmp` (indices explicit so the constructor pins the right `LeDec`; routed through `insert_pick` so the proof refines), `leall_trans` (a lower bound survives transitivity), `insert_leall` (insertion preserves a lower bound), `insert_sorted` (inserting into a sorted list keeps it sorted), `sort_sorted` (`Sorted(sort(l))` for every input). Uses the session's evidence-carrying-decision + helper-routing techniques in a brand-new domain. Idris `rel=same`.
- [x] **INSERTION SORT IS A PERMUTATION — full sorting correctness** (`insertion_sort_perm.cure`): the second half (sortedness was `insertion_sort`). Two lists are permutations iff every element occurs the same number of times, so count-preservation is the proof. `insert_count` (inserting x preserves the multiset: `count z (insert x l) = count z (x::l)`; the differing-order case swaps two conditional bumps via `bump_swap`), `sort_count` (the fold preserves every element's count, so `sort(l)` is a permutation of `l`). With `sort_sorted` this is FULL sorting correctness (sorted AND a permutation). First counting/multiset reasoning in the thread. Idris `rel=same`.
- [x] **TAIL-RECURSIVE REVERSE REFINES NAIVE REVERSE — verified optimization** (`fast_reverse.cure`): a concrete optimized=spec refinement over lists. Naive `rev` builds the reversal with append; tail-recursive `fast_rev` uses an accumulator (the real implementation). The ACCUMULATOR LEMMA `revacc_spec` (`revacc l acc = rev l ++ acc`) is the crux — induction with the cons step re-associating the append (`app_assoc`); `fast_rev_correct` falls out at the empty accumulator + append right-identity (`app_nil`). Rests on the append algebra; accumulator/refinement reasoning, distinct from the sorting proofs. Idris `rel=same`.
- [x] **MERGE OF TWO SORTED LISTS IS SORTED — the merge-sort core** (`merge_sorted.cure`): the first NON-STRUCTURAL recursion in the thread — merge interleaves two lists and descends on neither argument alone, only on their combined length. FINDING (verified against Idris, which rejects it identically): routing the whole merge through ONE comparison helper `merge_pick(x,r1,y,r2,·)` DEFEATS size-change totality in both checkers — the `LCons(y,r2)` reconstruction happens where `l2` is out of scope, so the reconstruct-equal that would witness the descent is lost, and an uncertified `merge` never δ-reduces in a proof. The total formulation (Idris-verified) is a THREE-function split: `merge` recurses on l1, `merge_into` recurses on l2 *with l2 in scope* (reconstruct-equal preserved → SCC certifies), and `merge_pick` routes the ordering decision as an explicit `LeDec` argument so it freezes with the decision VISIBLE (the same shape that makes `insert_pick` refine — A6 freezing hides an INLINE `case cmp` as a stuck scrutinee, invisible to `build_motive`, but a helper-as-argument stays exposed). The proofs mirror that exact three-function shape: `merge_leall` (a common lower bound survives the merge — the head-ordering obligation) and `merge_sorted` (Sorted ∧ Sorted ⟹ Sorted(merge)), each split into `merge_*`/`merge_into_*`/`merge_pick_*`, the `_pick` variant matching the decision PARAMETER directly. With `insertion_sort` this gives a second verified sort by an independent algorithm. Idris `rel=same`.
- [x] **MERGE IS A PERMUTATION OF THE CONCATENATION — full merge-sort correctness** (`merge_perm.cure`): the second half (`merge_sorted` was sortedness); together they are FULL merge correctness, mirroring the `insertion_sort` / `insertion_sort_perm` pair. Two lists are permutations iff every element occurs the same number of times, so `count(z, merge(l1,l2)) = plus(count(z,l1), count(z,l2))` — merge loses and invents nothing. Same three-function split as `merge_sorted`; `count` routes the equality test through `count_pick` so `eqn(z,w)` freezes VISIBLE and the proof can refine it (the merge_sorted A6 lesson again — an inline `match eqn` would be frozen as a stuck scrutinee, invisible to the motive). Arithmetic leans on `plus_zero` and `plus_succ_r` (plus recurses on its FIRST argument, so `plus(a, S(b))` is stuck and needs a lemma) — the `plus_succ_r` shuffle handles the case where the counted element equals a head coming from the SECOND list. On the Idris side the equality-test refinement needs `with (eqn z x)` (plain `case` won't refine `countPick ... (eqn z x)`), the exact analog of Cure's motive-refining `match`. Idris `rel=same`.
- [x] **A VERIFIED MERGEABLE MIN-HEAP — merge preserves the heap invariant** (`heap_merge.cure`): the priority-queue / scheduler primitive, and a demonstration that the `merge_sorted` three-function termination technique transfers from lists to TREES. Heap-merge is the same non-structural two-tree recursion as list merge (descends on neither tree alone, only on their combined size), so it reuses the identical three-function split — `merge` on h1, `merge_into` on h2 with h2 IN SCOPE, `merge_pick` routing the key comparison as an explicit `LeDec`. FINDING (the transfer's one subtlety): the LeNo case must recurse via `merge_into(x,l1,r1,r2)` (structural on `r2`, keeping the first heap's components), NOT `merge(Node(x,l1,r1), r2)` — reconstructing `Node(x,l1,r1)` and calling `merge` puts the reconstruction where `h1` is out of scope, so the SCC won't certify and `merge_pick` stays stuck (exactly the reconstruct-equal-must-be-in-scope rule, first seen on lists). The invariant `Heap(h)` is inductive: each node's key ≤ both child roots (`root_le`) and both children are heaps. `merge_heap` proves `merge` preserves it (three-function mirror `merge_*`/`merge_into_*`/`merge_pick_*`), the chosen root being the MIN of the two roots (`merge_root_le` — anything bounding both old roots bounds the new one, since the merge's root is one of them). Idris `rel=same`.
- [x] **A VERIFIED PRIORITY QUEUE — insert/delete_min preserve the invariant, find_min is the minimum** (`priority_queue.cure`): the priority-queue INTERFACE built on the mergeable heap, with its defining correctness. `insert` (= `merge` with a singleton) and `delete_min` (= `merge` the root's two heap subtrees) are defined purely via `merge` and PRESERVE the heap invariant (`insert_heap`, `delete_min_heap` — one-line corollaries of `merge_heap`). The load-bearing NEW result is MIN-AT-ROOT: `heap_below` proves a heap's root is a LOWER BOUND of EVERY element — an inductive `Below(b,h)` predicate (`b ≤ every element`) ranging over the whole tree — by induction on the `Heap` witness, using `root_le_trans` (order composes through the key: `b ≤ x` and `x` bounds a subtree's root ⟹ `b` bounds it). Hence `find_min_is_min`: `find_min(h)` is `≤` every element of `h`, the defining priority-queue guarantee that delete-min returns the smallest. Relies on the E-layer refining a SIBLING hypothesis (`hbh: root_le(b,h)=T`) when matching the GADT witness `sh: Heap(h)` on a variable index `h` — the same index-refinement that `sort_unique` needed, here confirmed on trees. Idris `rel=same`.
- [x] **HEAP-MERGE PRESERVES THE MULTISET — insert adds one, delete-min removes exactly the root** (`heap_multiset.cure`): the THIRD and final correctness axis of the mergeable heap (after the structural invariant and min-at-root). `merge_count` proves `count(z, merge(h1,h2)) = count(z,h1) + count(z,h2)` for every z, so the heap loses and invents nothing. It is the TREE analog of list merge-permutation (`merge_perm`), and strictly harder: each node contributes THREE summands (the root-equality indicator + the two subtree counts), so bridging the two sides needs plus-ASSOCIATIVITY on the LeYes side but a full `swap12` rearrangement (`plus(a, plus(b,c)) = plus(b, plus(a,c))`, itself `sym(assoc)` ∘ `cong_l(comm)` ∘ `assoc`) PLUS `plus_succ_r` on the LeNo side — where the new root comes from `h2` and lands in the SECOND plus-position, the asymmetry the two-summand list case never exposed. Same three-function split; `count` routes the equality test through `count_pick` so it refines (Idris needs `with (eq_dec z x)`). Corollaries `insert_count` (insert adds EXACTLY one element) and `delete_min_count` (delete-min removes EXACTLY the root) fall out as one-line `merge_count` applications. Completes a fully-verified priority queue: heap-order invariant preserved + min-at-root + multiset-faithful. Idris `rel=same`.
- [x] **COMPILER CORRECTNESS — a miniature verified compiler** (`compiler_correct.cure`): a fresh domain and thematically apt (Cure IS a compiler). Source `Expr` (Lit/Add/Mul) has a denotational semantics `eval`; the target is a STACK MACHINE with `exec` running a `Program` of push/add/mul instructions over a `Stack`; `compile` lowers an expression to stack code. `compile_correct` proves `exec(compile(e), s) = SCons(eval(e), s)` for EVERY expression and EVERY initial stack — the compiled code leaves exactly the evaluator's value on top, so the source (`eval`) and target (`exec`) semantics AGREE through `compile` (McCarthy–Painter / Hutton; the CompCert shape in miniature). Crux lemma `exec_append` (exec distributes over program concatenation: `exec(p ++ q, s) = exec(q, exec(p, s))`), proved by induction on `p` and casing the STACK in the binary-op instructions (a total `exec` handles stack underflow by pass-through; the proof's stack match refines both the folded `exec(AddOp::…, s)` applications so they reduce). `compile_correct` then follows by induction on `e` as a `trans` chain — unfold via `exec_append`, lift each subexpression's IH under the remaining program with `exec_cong`, and the final op reduces definitionally. `eval`/`exec` unfold so the `app(compile a, …)`-shaped proof converts against the folded `compile(Add a b)` goal with no rewrite-syntactic-match issue. Idris `rel=same`.
- [x] **A VERIFIED OPTIMIZING COMPILER — constant folding + its composition with compilation** (`optimizer_correct.cure`): the SECOND pillar of verified compilation (semantic preservation of a TRANSFORMATION), composed with the first into an end-to-end optimize-then-compile pipeline. CONSTANT FOLDING (`fold`, via `fold_add`/`fold_mul`) evaluates constant subexpressions at compile time. `fold_preserves` proves `eval(fold(e)) = eval(e)` — the optimization never changes a program's value — by induction with per-node lemmas `fold_add_correct`/`fold_mul_correct` (folding a binary node computes the same value whether or not both operands collapsed to literals — five shape cases, each `reflexive` once `fold_add`'s inline matches reduce) and `plus`/`times` congruence. `opt_correct` then COMPOSES `fold_preserves` with `compile_correct` (the stack-machine compiler, reproved here) as a two-step `trans`: `exec(compile(fold(e)), s) = SCons(eval(fold(e)), s)` [compile_correct] then `= SCons(eval(e), s)` [`scons_cong` ∘ `fold_preserves`]. So compiling the OPTIMIZED program still yields the ORIGINAL expression's value — a fully verified optimizing pipeline, demonstrating the MODULARITY of verified compilation (optimize ∘ compile). Idris `rel=same`.
- [x] **COMPILER CORRECTNESS WITH VARIABLES + ENVIRONMENTS** (`compiler_var.cure`): a real step up from the closed-expression compiler. The source `Expr` gains DE BRUIJN VARIABLES (`Var(k)` reads the k-th environment entry, out-of-range → Z); the target stack machine gains a `Load(k)` instruction that pushes a variable from a read-only ENVIRONMENT threaded through `exec` (`exec(p, env, s)`). `compile_correct` proves `exec(compile(e), env, s) = NCons(eval(e, env), s)` for every expression, every variable environment, and every initial stack — the classic "compiling with an environment" theorem. The `Var` case is discharged by the `Load` instruction reading `lookup(k, env)` (reflexive, since both sides reduce to `NCons(lookup(k,env), s)`); the rest is the McCarthy–Painter proof (crux `exec_append`, then a `trans` chain by induction on `e`) with `env` threaded UNCHANGED through every step — `exec`/`exec_append`/`exec_cong` all carry it read-only, so the environment adds a parameter but no new proof obligation beyond the `Var`/`Load` base case. `NL` (a `Nat` list) serves as both the environment and the stack. Idris `rel=same`.
- [x] **VERIFIED BINARY ARITHMETIC — positional numerals, increment, round-trip, ripple-carry addition** (`binary_arith.cure`): a fresh domain (positional numeral systems). Naturals are bit lists LSB-first (`Bin`), denoted by `to_nat`. Proved FAITHFUL — `inc_correct` (the binary successor denotes the Nat successor) and `from_nat_correct` (round-trip: every natural round-trips through its binary form) — and then the real work: RIPPLE-CARRY ADDITION. `addc` is the bit-by-bit adder with an incoming carry, on a full adder `badd` (sum = parity of the three inputs) / `bcarry` (carry-out = majority); `fa` proves the defining identity `sum + 2·carry = c + x + y` (eight cases, each reflexive). `addc_correct` proves `to_nat(addc(c,a,b)) = c + to_nat a + to_nat b` by induction, its binary-node case a long `plus`/`double` algebra chain: distribute `double` over `+` (`double_plus`), substitute the full-adder identity, and reassociate (`rearrange4` swaps the middle two summands of `(w+x)+(y+z)`); `add_correct` falls out at carry `O`. Key move: `double` is DIRECTLY recursive (`double(S k) = S(S(double k))`) rather than `plus(n,n)`, so it reduces cleanly under the algebra (`double_succ` becomes definitional). Both sides accepted first try. Idris `rel=same`.
- [x] **VERIFIED BINARY MULTIPLICATION** (`binary_mul.cure`): the hardest piece of the binary-arithmetic development, built on the ripple-carry adder. `bmul` is the shift-and-add multiplier — `(2·a2)·b = 2·(a2·b)` (even bit) and `(2·a2+1)·b = b + 2·(a2·b)` (odd bit: one copy of `b` added atop the doubled partial product via the VERIFIED adder `add`/`add_correct`). `mul_correct` proves `to_nat(bmul a b) = to_nat a · to_nat b` by induction on `a`'s bits. New algebra it forces: `times_plus_l` (right-distributivity of `·` over `+`), `double_is_plus` (`double x = x + x`, reconciling the directly-recursive `double` with the additive one) and `double_times` (`2·(m·n) = (2·m)·n`, so the even-bit doubling commutes into the left factor — proved `2·(mn) = mn+mn = (m+m)·n = (2m)·n` via `times_plus_l`). Reuses the entire ripple-carry `add`/`add_correct` machinery (increment, full adder, carry algebra) verbatim; the odd-bit case chains `add_correct` with `plus_cong_r` over `double_times`. Completes verified binary arithmetic: representation faithful, addition and now multiplication correct. Idris `rel=same`.
- [x] **BRAUN TREES — balanced-by-construction: insert increments size AND preserves balance** (`braun_tree.cure`): a fresh data-structure domain, distinct from the min-heap in that the invariant is about SIZE SYMMETRY, not element order. The Braun invariant `Bal(m,n)` on subtree sizes is `m = n` OR `m = S(n)` (left equal to, or exactly one larger than, right); `Braun(t)` requires both subtrees Braun and balanced at every node. The classic Braun `insert(x, Node(y,l,r)) = Node(x, insert(y,r), l)` seats x at the root, pushes the old root into the RIGHT subtree, and SWAPS the subtrees. `size_insert` proves `size(insert(x,t)) = S(size(t))` (its node case reconciling the swap via `plus_comm`); `braun_insert` proves the invariant is preserved — the crux is that the swap TRADES the balance disjuncts: casing the incoming `Bal(size l, size r)` witness, a `BalEq` (balanced) node yields a `BalHi` (left-heavy) node and vice versa, since the new left is `insert(y,r)` of size `S(size r)` and the new right is the old `l`. On the Idris side the forced index binders of the `Bal` constructors must be dotted (`BalEq _ _ e`) since they unify with the computed `size l`/`size r`. Idris `rel=same`.
- [x] **TWO-PHASE COMMIT SAFETY — validity, reflection, atomicity** (`two_phase_commit.cure`): a distributed-protocol correctness proof (atomic commitment), returning to the OTP/distributed theme. Participants vote; the coordinator commits IFF every vote is yes, else aborts (`decide = decide_b(all_yes(votes))`). Proved: VALIDITY both directions — `commit_valid` (unanimity ⟹ Commit, one `rewrite`) and `valid_commit` (Commit ⟹ unanimity, via the CONVOY pattern `valid_commit_b` so matching the boolean refines the decision hypothesis, the Abort branch refuting `Abort = Commit`); a REFLECTION between the boolean `all_yes` check and an inductive `HasNo` ("some participant voted No") — `hasno_sound` (a No falsifies unanimity) and `hasno_complete` (a failed unanimity exhibits a concrete dissenter), the two directions bridging the decidable check and the existential witness; `abort_on_no` (any No forces Abort — no participant commits over a veto, a `rewrite` on `hasno_sound`); and ATOMICITY / uniform agreement — `atomicity` proves the outcome vector is UNIFORMLY the coordinator's decision (`AllSame(decide(vs), outcomes(decide(vs), vs))`), so no participant commits while another aborts, generalized over an arbitrary fixed decision by `all_same_replicate`. Idris `rel=same`.
- [x] **MODULAR CONGRUENCE — an equivalence relation that respects addition (Z/n)** (`mod_congruence.cure`): a fresh NUMBER-THEORY domain. `a ≡ b (mod n)` is defined by a witness pair `p, q` with `a + p·n = b + q·n` (a and b differ by a multiple of n). Proved an EQUIVALENCE RELATION — reflexive (`cong_refl`, take `p=q=0`), symmetric (`cong_sym`, swap the witnesses + `sym`), transitive (`cong_trans`, COMBINE the witnesses `(p1+p2, q1+q2)`; the equation follows by a ten-step algebra chain that distributes the combined multiple over `+` and re-associates BOTH offsets around the shared middle term `b`, so no cancellation is needed) — that RESPECTS ADDITION (`cong_add`: `a≡a' ∧ b≡b' ⟹ a+b ≡ a'+b'`, interleaving the two offsets via `rearrange4` then applying both witness equations with `plus_cong2`). So addition is well-defined on residue classes — the foundation that Z/n is a well-defined additive structure. The witness formulation (vs a reflexive-symmetric-transitive-closure inductive) is what keeps transitivity clean: combining offsets sidesteps the awkward "remove an `n`" inversion. Reuses the `plus`/`times` algebra (right-distributivity `times_plus_l`, `plus_assoc`/`plus_comm`, `rearrange4`). Idris `rel=same`.
- [x] **MODULAR ARITHMETIC IS A COMMUTATIVE RING — congruence respects multiplication** (`mod_ring.cure`): extends `mod_congruence` (where addition-respect made Z/n an additive structure) to the FULL ring by proving congruence respects MULTIPLICATION. Builds the whole commutative-semiring `times` algebra from scratch: `times_zero_r`, `times_succ_r`, `times_comm`, `times_assoc`, and BOTH distributivities (`times_plus_l` right, `times_plus_r` left — the latter derived from the former by commutativity). The one-sided laws are `cong_times_r` (`a≡a' ⟹ a·b ≡ a'·b` — multiply the witness equation `a + p·n = a' + q·n` by `b`, carrying the multiple through via `times_swap`: `(p·n)·b = (p·b)·n`, itself `assoc`∘`comm`∘`assoc`) and `cong_times_l` (`b≡b' ⟹ a·b ≡ a·b'`, the mirror via left-distributivity); `cong_mul` composes them through `cong_trans` as `a·b ≡ a'·b ≡ a'·b'`. With `cong_add`, both `+` and `·` are well-defined on residue classes — Z/n is a well-defined commutative ring. Deep proof chains generated programmatically for paren-balance. Idris `rel=same`.
- [x] **NEGATION NORMAL FORM IS MEANING-PRESERVING** (`nnf_correct.cure`): a fresh domain — boolean logic / normal forms. Propositional formulas (`FVar`/`FNot`/`FAnd`/`FOr`) have a truth-value semantics `eval` under an assignment; `nnf` pushes every negation DOWN to the variables, with a companion `nnf_neg(f)` computing the NNF of `¬f`. `nnf_correct` proves `eval(nnf(f), env) = eval(f, env)` for every formula and assignment — normalizing to NNF never changes meaning — by MUTUAL induction with `nnf_neg_correct` (`eval(nnf_neg(f), env) = ¬eval(f, env)`). The pushed-negation cases are discharged by the De Morgan laws (`dm_and`/`dm_or`) and double-negation (`notnot`) — e.g. `nnf_neg(FAnd a b) = FOr(nnf_neg a, nnf_neg b)` proved `orb(¬eval a, ¬eval b) = ¬andb(eval a, eval b)` via `sym(dm_and)`; the double-negation case `nnf_neg(FNot g) = nnf g` uses `notnot`. Connective cases lift the IHs through `andb_cong2`/`orb_cong2`. The mutual `nnf`/`nnf_neg` recursion and mutual `nnf_correct`/`nnf_neg_correct` proof both certify. Idris `rel=same`.
- [x] **RUN-LENGTH ENCODING ROUND-TRIPS** (`rle_roundtrip.cure`): a fresh DATA-COMPRESSION domain. `encode` groups consecutive equal elements of a list into `(value, positive-count)` runs — `push` extends the leading run when the new head matches its value (deciding equality with an evidence-carrying `EqDec`, routed through the `push_pick` helper so the proof can refine the folded `push`/`decode`); `decode` expands them via `replicate` + `app`. `rle_roundtrip` proves `decode(encode(l)) = l` for EVERY list — the encoder is LOSSLESS. The crux `decode_push_pick` proves decoding a pushed element equals consing it onto the rest's decoding: the MATCH case uses the equality WITNESS `x = y` to rewrite `replicate(x,n)` to `replicate(y,n)` (`replicate_cong` → `app_cong_l` → `lcons_snd_cong`), the non-match case is reflexive (the `replicate(x, S Z)`-then-`app` reduces definitionally to a cons). `rle_roundtrip` then chains `decode_push` with the induction hypothesis. Idris `rel=same`.
- [x] **VERIFIED EUCLIDEAN DIVISION — the division algorithm with its full spec** (`euclid_div.cure`): a classic hard result. For every `a` and divisor `S b0`, `divmod` yields `(q, r)` with `q·(S b0) + r = a` AND `r < S b0`. CORRECT BY CONSTRUCTION: `dm` returns a `DivResult` that BUNDLES q, r and both proofs, indexed by the INPUTS `a, b0`. **Key finding**: indexing the certificate by the STUCK division-RESULT value (a `QR`-value index) fails — the kernel can't structurally unify a stuck `dm(...)` with `MkQR(q,r)` when matching (function-application indices don't invert), so the certificate must ride WITH the function (correct-by-construction), not be a separate spec over the result. Fuel-bounded recursion with a SUFFICIENCY argument (`div_fuel_ok`: `a ≤ S f ∧ S b0 ≤ a ⟹ a - S b0 ≤ f`, via `monus_lt` + `le_trans`); the strict comparison is evidence-carrying (`LtDec`), routed through `dm_pick` so the recursive step's reduction stays visible (the A6 decision-as-argument lesson, here also letting the recursive `DivResult` match refine the folded `dm_pick`). The recursive step reconstitutes `a = S b0 + (q·(S b0)+r) = (S q)·(S b0)+r` via `plus_monus` (`b ≤ a ⟹ b + (a - b) = a`). Fuel = a is always sufficient (the quotient is ≤ a), so `divmod(a, b0) = dm(a, a, b0, leq_refl(a))` needs no extra hypothesis. Diagnosed the two failed formulations (auto-implicit indices; stuck-value index) with the branch-type instrumentation before landing correct-by-construction. Idris `rel=same`.
- [x] **GRAPH REACHABILITY — transitivity, additive walk composition, closure = bounded-walk existence** (`graph_reach.cure`): a fresh GRAPH-THEORY domain. A graph is an adjacency test `edge`; `Reach` is unbounded reachability (reflexive-transitive closure), `ReachN(k,u,v)` is reachability in EXACTLY k steps. Proved: `reach_trans` (reachability is transitive — with `RRefl`, a preorder — by concatenating the two walks); `reachn_trans` (counted reachability COMPOSES ADDITIVELY — a j-step then a k-step walk is a `(j+k)`-step walk, the lengths ADD, tracked exactly in the `plus(j,k)` index); and `reach_to_ex`/`ex_to_reach` (unbounded reachability EQUALS the existence of a bounded walk, both directions, via a packed `ExReachN` witness) — the declarative closure and the operational step-counted walk coincide. Every proof is edge-AGNOSTIC: it only threads the `edge(u,mid)=T` witnesses, never inspecting the concrete graph, so the results hold for any adjacency relation. On the Idris side the reflexive constructors' forced indices must be reused on the LHS (`reachTrans x x w (RRefl x)`, etc.). Idris `rel=same`.
- [x] **SORTING IS UNIQUELY SPECIFIED — the uniqueness half of sorting correctness** (`sort_unique.cure`): the sort algorithms (`insertion_sort`, `merge_sorted`) prove a sorted permutation EXISTS; this proves it is the ONLY one — two lists that are both sorted and have the SAME MULTISET (equal element counts everywhere) are EQUAL, so any correct sort is forced to produce this exact list. Structurally recursive (no termination wall), but it forces a real three-way interaction between sortedness, multiset counts, and order antisymmetry. Proof: heads must be equal — each list's head bounds the other's head as an element PRESENT in it (membership encoded as `count > 0`), giving `leq` both ways, hence equal by antisymmetry (`leq_antisym`); the tails then have equal counts and are sorted, so equal by induction. NEW machinery this probe introduces to the thread: (a) a **universally-quantified count-equality hypothesis** as a dependent function argument `h: (z: Nat) -> Equivalent(Nat, count(z,xs), count(z,ys))`, rebuilt for the tail via a lambda + S-injectivity (`s_inj` via `pred_cong`); (b) an evidence-carrying equality decision `EqDec` whose `EqNo` carries `eqn(x,y)=F`, so `count_cons_self` discharges the impossible `eq_dec(y,y)=EqNo` branch by contradicting `eqn_refl`; (c) the **convoy pattern** for hypothesis refinement — `leall_count_le`/`head_le`/`tail_count`/`count_cons_self` all route the equality decision through a `_pick` helper that RETURNS A FUNCTION of the count hypothesis, because a top-level match on a computed scrutinee (`eq_dec …`) leaves the folded `count` opaque (the merge_sorted A6 lesson, now applied to refine a HYPOTHESIS not just the goal). Impossible `Z = S(_)` branches discharged by `match` on the absurd equation. On the Idris side the convoy is just currying (`... -> (d : EqDec) -> (count… = S k) -> …`) and the impossibles are `case … of Refl impossible`. Idris `rel=same`.
- [x] **VECTOR CLOCKS — the causal order is a PARTIAL ORDER** (`vector_clock.cure`): opens LOGICAL TIME, the causality substrate for message-passing / BEAM systems, as a fresh domain. A vector clock is one `Nat` counter per process (`VC = VNil | VCons(Nat, VC)`); the causal happens-before-or-equal order `vc_le` is the POINTWISE `Nat` order (`andb(leq(x,y), vc_le(u2,v2))`, F on a dimension mismatch). Proved a genuine partial order: `vc_le_refl` (reflexive), `vc_le_trans` (transitive — componentwise `le_trans`), `vc_le_antisym` (antisymmetric — componentwise `leq_antisym`, so `u ≤ v ∧ v ≤ u ⟹ u = v` structurally via `vcons_cong`). The induced STRICT order `vc_lt(u,v) = andb(vc_le(u,v), notb(vc_eq(u,v)))` is irreflexive (`vc_lt_irrefl`, no event strictly before itself, via `vc_le_refl` + `vc_eq_refl`), and CONCURRENCY `vc_conc(u,v) = andb(notb(vc_le(u,v)), notb(vc_le(v,u)))` is symmetric (`vc_conc_sym` via `andb_comm`). The componentwise proofs decompose the pointwise `andb` into its two conjuncts (`andb_true_l`/`andb_true_r`) and recombine (`andb_intro`), reusing the `Nat` order lemmas from the sorting corner — no convoy/termination subtlety, just clean structural inductions + boolean algebra. Idris `rel=same`.
- [x] **VECTOR CLOCKS FORM A JOIN-SEMILATTICE — the receive-merge is the causal LEAST UPPER BOUND** (`vector_clock_join.cure`): the operational algebra beneath the causal order, and the algebraic core of CRDT convergence. The receive-merge of two vector clocks is the componentwise max (`vmax`); this proves it is exactly the JOIN of the causal order `vc_le` — `vmax_ub_l`/`vmax_ub_r` (both operands ≤ the merge, so after receiving the clock dominates BOTH the local history and the sender's) and `vmax_lub` (any common upper bound dominates the merge, i.e. leastness). Merge is also a SEMILATTICE — commutative (`vmax_comm`), idempotent (`vmax_idem`), associative (`vmax_assoc`) — which is precisely STRONG EVENTUAL CONSISTENCY: replicas that merge the same set of updates converge regardless of message ORDER, DUPLICATION, or GROUPING. Plus `tick_mono` (`v ≤ tick(i,v)` — a local event ticks the process's own component and the clock only advances, so a process's clock is monotone along its execution). The componentwise proofs rest on a small Nat-max algebra newly built here: `le_max_l`/`le_max_r` (max is an upper bound), `max_lub` (max is least), `max_comm`/`max_idem`/`max_assoc` (Nat max is itself a semilattice). `VNil` is the bottom clock (below every clock — makes the join laws hold at every arity with no length-mismatch side conditions). Reuses the `andb` conjunct-decomposition and `Nat` order lemmas from the sorting/partial-order corner. Idris `rel=same`.
- [x] **STRICT CAUSAL PRECEDENCE IS A STRICT PARTIAL ORDER + local events strictly advance the clock** (`vector_clock_strict.cure`): completes the order-theoretic picture of logical time and takes the first operational step toward Fidge–Mattern. The "happens-strictly-before" relation is `vc_lt(u,v) = andb(vc_le(u,v), notb(vc_le(v,u)))` (the poset strict order `u ≤ v ∧ ¬(v ≤ u)` — cleaner than `u ≤ v ∧ u ≠ v`, avoiding all `vc_eq` bookkeeping). Proved IRREFLEXIVE (`vc_lt_irrefl`), ASYMMETRIC (`vc_lt_asym` — no causal 2-cycles), and TRANSITIVE (`vc_lt_trans` — causal precedence composes). NEW machinery: **`bool_mt`, a boolean modus-tollens** (`(b=T ⟹ c=T)` and `c=F` force `b=F`), which refutes the transitivity's reverse leg `¬(w ≤ u)` WITHOUT boolean blindness — were `w ≤ u`, then `w ≤ v` by `vc_le_trans`, contradicting `¬(w ≤ v)`. `bool_mt` must be stated with the **convoy pattern** (match the boolean, RETURN a function of the implication hypothesis) because a plain `match b` does not refine a sibling hypothesis whose type mentions `b` (the sort_unique lesson; in Idris the clause-based match refines it directly, so `boolMt` is flat/curried there — same type). Then the operational leg: `tick_strict` proves `v ≺ tick(i,v)` — a local event ticks the process's own component and strictly advances the clock — using an inductive `InRange(i,v)` witness that the index is within the clock's dimension (so the tick genuinely changes a component; `tick_gt` shows the ticked clock is not `≤` the old one via `leq_succ_self_f`). So successive events on ONE process are strictly ordered by their clocks — the program-order soundness of vector clocks. Idris `rel=same`.
- [x] **THE CLOCK CONDITION — Fidge–Mattern SOUNDNESS** (`fidge_mattern.cure`): the capstone of the logical-time development. Causal precedence (Lamport happens-before) is modelled INDUCTIVELY over clock-stamped events by `HB(a,b)`, generated by the two OPERATIONAL rules — `HBTick` (a local event: `HB(v, tick(i,v))` with an `InRange` witness — program order) and `HBRecv` (a receive that merges the sender's timestamp then ticks: `HB(m, tick(j, vmax(v,m)))` — message order) — closed under `HBTrans`. `hb_sound` proves `HB(a,b) ⟹ vc_lt(a,b) = T`: if event a causally precedes b, its vector clock is STRICTLY less. So comparing timestamps SOUNDLY detects causality — if `VC(a) ⊀ VC(b)` then a did NOT cause b, the basis of vector-clock causality tracking in distributed / BEAM systems. The proof composes the ENTIRE logical-time corner: the local leg is `tick_strict`; the receive leg chains sender's-stamp-≤-merge (`vmax_ub_r`) with merge-≺-ticked-clock (`tick_strict`) via a new mixed-transitivity lemma `le_lt_trans` (`m ≤ a ∧ a ≺ b ⟹ m ≺ b`, itself using `bool_mt`); the transitive closure is `vc_lt_trans`. This is the standard, cleanly-provable half of the Fidge–Mattern characterization; the converse COMPLETENESS direction (`VC(a) < VC(b) ⟹ a → b`, which needs the clock to encode enough per-process history) is the deeper direction — now DONE (next bullet). Idris `rel=same`.
- [x] **Fidge–Mattern COMPLETENESS — comparing clocks is COMPLETE for causality** (`fm_prefix_count.cure` + `fidge_mattern_complete.cure`): the converse of the clock condition, the deferred hard direction. `VC(a) ≤ VC(b) ⟹ a` causally precedes-or-equals `b`. This is FALSE on raw vector clocks (unrelated stamps compare), so it needs event IDENTITY and the RETROSPECTIVE clock `VC(e)[i] = #{ process-i events causally ≤ e }`. The core is `fm_prefix_count`'s **threshold lemma**: an event's VC coordinate is the POPCOUNT of a downward-closed "in-the-causal-past?" bitmask (`Prefix` = a run of Ts then Fs, forced by program order), and `threshold` proves `nth(mask,k) = (k < popcount)` — position k is in the past exactly when k is below the count. `fidge_mattern_complete` then builds the event layer: a full vector clock `Vec`, its pointwise order `vec_le`, coordinate projection (`vec_le_proj`), and the DIAGONAL fact `VC(a)[proc a] = S(pos a)` (an event's own coordinate counts itself and all earlier same-process events). `completeness` pivots on coordinate `p = proc(a)`: projecting `vec_le(VC a, VC b)` there gives `S(pos a) ≤ popcount(b's mask)`, which `threshold` turns into `nth(mask, pos a) = T` — membership in b's causal past. Discharged on a concrete CROSS-PROCESS instance (`ex_complete`: a message 0→1, a=(proc 0,pos 1) ⟶ b=(proc 1,pos 1), VCs [2,0] ≤ [2,2]) so it is non-vacuous. With soundness this is the FULL Fidge–Mattern characterization: vector-clock comparison is both sound and complete for causality. Both files Idris `rel=same`; zero kernel/elaborator change. (The `_pick`/A6 discipline was not needed — every scrutinee is a constructor-headed variable, refined by the sibling-refinement fix `ed3a0485`.)
- [x] **Async restart must FLUSH the in-flight buffer** (`restart_orphan.cure`): on the async dimension, a config carries a message buffer (`BEmpty`/`BFull`). `flush_is_fresh` proves resetting both participants to duals AND emptying the buffer is a well-formed fresh session for every protocol; `noflush_orphans` proves resetting the locals but KEEPING a non-empty buffer `BFull(m)` leaves the config not-fresh for every protocol — the buffered message is an ORPHAN no fresh participant awaits (helper `noflush_orphans_tail` cases the stuck `compatible` so `andb(x, False)` reduces). So a correct restart of an async session flushes its buffer, not merely resets protocol state — ties async orphan-freedom to the restart action.
- [x] **Lifecycle termination × session completion (conservation)** (`session_lifecycle.cure`): a gen_server carries a session and performs one callback per protocol message; a run fragment `Serve(pre, post)` drives the state. `conservation` proves `run_len(s) + proto_len(post) = proto_len(pre)` — steps taken plus obligations remaining equals the total protocol length, by induction (each step decrements remaining and increments count in lockstep). Corollary `complete_run`: a run reaching `LEnd` performs EXACTLY `proto_len(pre)` steps — every session obligation discharged once, none skipped (no stuck/deadlocked server) or doubled. The `Terminated(l)` index enforces NORMAL-stop-only-at-`LEnd` (`clean_shutdown`) while `TCrash` may stop at any state (`crash_anytime`) — the lifecycle asymmetry. FINDING: Idris auto-bound implicits are erased (multiplicity 0), so threading `post` through the recursive proof gives `post is not accessible`; explicit `{pre : Local} -> {post : Local}` named-implicit binders fix it (faithful — same theorem). Idris `rel=same`.
- [x] **Supervised-session liveness — progress completes or intensity abandons** (`session_liveness.cure`): ties restart INTENSITY to session PROGRESS in one shared state `Run(orig, cur, budget)`. A PROGRESS event advances `cur` toward `LEnd` (→ `Completed`), a CRASH resets `cur` to `orig` and consumes budget (→ `Abandoned` at zero). `progress_completes` (`S(proto_len cur)` progress steps reach `Completed` — a progressing session terminates in success, induction on the protocol, message-agnostic) and `crash_abandons` (`S(budget)` crashes reach `Abandoned` — a persistently-crashing session exhausts its restart intensity and is abandoned, never retried forever, induction on budget), plus all four cross-absorbing lemmas (`Completed`/`Abandoned` ignore a spurious crash/progress — no resurrection under either event). So a doomed supervised session cannot loop forever: it either completes or is abandoned. Idris `rel=same`.

### B9. Out of scope (confirmed)
- Static DEADLOCK / LIVENESS — undecidable, a runtime result; a DDMon-style probe layer if ever wanted, orthogonal to the type system.

---

## "Beyond the papers" — the delta already achieved

| Property | Papers | Here |
|---|---|---|
| Operational preservation for mailbox-typed configs | Proof of Delivery DEFERS it | DONE (tag fragment), tied to inference |
| Mechanized | de'Liguoro–Padovani / NVLang = paper/sketch proofs | DONE, kernel-certified + Idris cross-checked |
| Dependent + LINEAR reply (`ReplyOf`, exactly-once) | none | DONE, intrinsic + operational |
| Progress + multi-pid interleaving | NVLang single-actor, no progress | DONE |
| Timers, call-totality, effect-honesty | no paper | DONE |
| Inference adequacy (infer ⇒ operational safety) | literature only STATES it | DONE, sequential + branching |
| Recursion fixpoint inference (`infer(BRec)` = least fixed point, operationally safe) | literature STATES the lfp exists; leaves coverage/adequacy for recursion open | DONE — syntax-derived transfer, finite Kleene stabilization, coverage, adequacy, principality, all kernel-certified + Idris cross-checked (finite universe) |

## Critical path to "FULL"
B2 (recursion fixpoint) is **COMPLETE** (transfer derivation → monotonicity → finite Kleene
stabilization → coverage → operational adequacy → principality; `otp_finite_fixpoint` /
`otp_interface_bridge` / `otp_recursive_transfer` / `otp_recursive_adequacy` / `otp_recursive_run`),
for the finite (bounded-tag) universe. B3 (counting fragment) is the largest remaining piece and is
partly EXTERNAL (Presburger/Z3). B5–B8 are depth/fidelity extensions. B1 is cleanup + ergonomics.
Everything in B1/B2/B4/B5/B6/B7/B8 is in-Cure provable; only B3's solver step sits outside the TCB.

## Usable-algebra spine (2026-07-19) — dependent call boundary

- [x] **The DEPENDENT CALL BOUNDARY** (`dep_call_boundary.cure`, rel=same): `call(server, req) -> ReplyOf(req)` — ONE server answering heterogeneous requests, each at its own reply type, checked at the call site. This is what `Std.Otp.call` (uniform `Effect(r)`) lacks; the client half of the integration spine. The return-only-implicit worry that kept `call` uniform does NOT apply — `ReplyOf(req)` is a function of the EXPLICIT request argument, so it is determined. A dependent server carries a total handler `(r) -> ReplyOf(r)`; `call` dispatches through it. Two calls to one server resolve to two distinct reply types. Type-sound; the `Otp.Meta.Proof` reply rule is the same discipline at the reply end.
- [ ] **Stdlib promotion of `call_dep` — WALLED (kernel uniform-parameter check).** A codegen-able dependent server must be INDEXED by its reply family (`rep : q -> Type`) so `call_dep` recovers it; a phantom typealias fails (higher-order unification). But `type DepGenServer(q: Type, rep: (q)->Type)` — a runtime datatype with a FUNCTION-VALUED parameter — trips `kernel.ex:check_uniform_params` at position 0. Fork: (a) associated-type-family `interface Request(q){ ReplyOf : q -> Type }` (server indexed by `q` only, codegen-safe); (b) monomorphization (parked backend initiative); (c) relax `check_uniform_params` for dependent param telescopes (TCB HARD-STOP); (d) accept static-only guarantee + a uniform-reply runnable demo. Item 3 (mailbox exhaustiveness) is a pure proof and unblocked.

## Usable-algebra spine (2026-07-19, cont.) — server side runs under real OTP

- [x] **Dependent `handle_call` SHAPE proven** (`dep_handle_call.cure`, rel=same): `handle_call(request, state) : Tuple(Atom, ReplyOf(request), State)` — the reply typed per request, checked per branch. Same discipline as `Otp.Meta.Proof.handle`, wrapped in `{:reply, _, state}`.
- [x] **Dependent-reply gen_server RUNS end-to-end** (`test/cure/e2e/dependent_gen_server_test.exs`): a gen_server written in Cure via `lift module`, compiled to a real BEAM callback module, driven by stock `gen_server:start_link` + `gen_server:call`. One server answers `GetCount → RCount ({:Cnt,n})` and `Ping`/`SetName → RAck (:Ack)` — two distinct reply types checked at compile time — with state mutation across calls. The dependent reply is purely static (erases), so it lowers to an ordinary OTP callback module. The dependent tuple is built in a `reply_for` helper (keeps the `Sigma` in checking position; a bare tuple literal in the callback body hits `ctor_requires_checking_mode` on the lift-module path). This is the SERVER half of the spine + the empirical "usable to talk to OTP" demo (algebra items 19 + 4).
- Note: a codegen var-name collision (a `fresh_var("V")` field binder could equal a positional param `V<pos>`, corrupting an unused-payload-var + tuple-body match branch → `CaseClauseError`) surfaced while running this demo and was fixed on `main` (`fresh_var` now inserts an underscore: `V_<n>`). Merged in. The type-only oracle never caught it because proof modules are never RUN.

## Mailbox exhaustiveness / in-protocol progress (2026-07-19, item 3)

- [x] **Mailbox exhaustiveness ⟹ in-protocol progress** (`mailbox_exhaustive.cure`, rel=same): a receive loop follows a SESSION (sequence of expected receives); the MAILBOX is the set of tags it handles. `exhaustive_progress` proves coverage ⟹ `steps(set,s) = len(s)` — a mailbox that covers the session drives the loop to completion, discharging every receive obligation, NEVER stuck. `steps_le_len` proves the dual bound (`steps ≤ len` always, no run overruns its obligations), so coverage is exactly the condition for MAXIMAL progress. `stuck_example` witnesses the necessity: mailbox `{TA}` on a session expecting `TB` stalls at 0 steps. The per-message handled decision is routed through `steps_pick` (visible argument) so the proof refines the otherwise A6-frozen recursive `steps` call (the `_pick` idiom). Complements the session-progress corner (`session_lifecycle` conservation, `session_liveness` progress/abandon) with the HANDLER-COVERAGE angle: not just "the protocol advances" but "the mailbox handles everything the protocol admits, so it can't get stuck."

## The behavior sugar (2026-07-19, item 1) — the spine as one form

- [x] **`behavior` macro — one-form dependent gen_server** (`lib/std/actor.cure` `macro behavior` + `derive_behavior_family`; e2e `test/cure/e2e/behavior_macro_test.exs`): `behavior N state S request Q reply R / body <Q,R,aux> / handle <arms>` derives a complete gen_server whose `handle_call` answers each request at its OWN reply type `R(request)`. The user writes only protocol logic; each `handle` arm gives just the reply value, the macro wraps it into `%[:reply, value, state]`, synthesises the `reply_for` helper (dependent `Sigma` in checking position), emits every callback + `start_link`, and drops the `body` declarations into the lifted module so `Q`/`R` resolve. State held constant (reply/query servers). Built PROGRAMMATICALLY (like `derive_actor_family_cases`), sidestepping the three `becomes`-template walls (delayed-raw-into-fn crash, lift-module scoping, `<T:Type>` capture non-substitution). e2e compiles a 3-request query server and drives it via real `gen_server:call`, resolving three DISTINCT reply types from one server. Completes the spine (client half = `Std.Otp.call_dep`). Full gate 5025/0; actor goldens unaffected.

**All four OTP usable-algebra items are now DONE:** dependent client `call_dep`, dependent server `handle_call`, mailbox-exhaustiveness/progress, end-to-end demos — and the `behavior` sugar ties them into one surface form.
